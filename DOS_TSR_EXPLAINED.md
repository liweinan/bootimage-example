# DOS TSR（Terminate and Stay Resident）程序详解

**版本**: 1.0  
**日期**: 2026-01-30

> 📚 **文档导航**: [返回总索引](DOCUMENT_INDEX.md) | [阅读指南](READING_GUIDE.md) | [DOS 引导加载程序](DOS_BOOTLOADER.md) | [实模式与 TSS](X86_64_TSS_AND_IST.md#20-前置背景实模式无硬件任务切换)

---

## 目录

1. [什么是 TSR？](#1-什么是-tsr)
2. [TSR 的工作原理](#2-tsr-的工作原理)
3. [驻留机制详解](#3-驻留机制详解)
4. [TSR 常钩住的中断](#4-tsr-常钩住的中断)
5. [著名的 TSR 程序](#5-著名的-tsr-程序)
6. [TSR 安装代码示例](#6-tsr-安装代码示例)
7. [TSR 的问题与局限](#7-tsr-的问题与局限)
8. [TSR 与现代操作系统对比](#8-tsr-与现代操作系统对比)
9. [实模式的任务切换本质](#9-实模式的任务切换本质)
10. [演示程序](#10-演示程序)

---

## 1. 什么是 TSR？

TSR（Terminate and Stay Resident，终止并驻留）是 DOS 时代实现"后台程序"的核心技术。

**DOS 本身是单任务操作系统**，但 TSR 本质上就是一种多任务程序，通过**替换/钩住 DOS 的中断服务程序**来实现后台功能。

| 特性 | 说明 |
|------|------|
| **全称** | Terminate and Stay Resident（终止并驻留） |
| **时代** | DOS 时代（1980s-1990s） |
| **本质** | 通过中断钩子实现的"后台程序" |
| **激活方式** | 被动激活（通过中断触发） |

---

## 2. TSR 的工作原理

### 2.1 中断钩住机制

TSR 通过修改中断向量表（IVT）来"钩住"系统中断：

```
┌─────────────────────────────────────────────────────────┐
│  正常的中断处理                                         │
│  INT 09h (键盘) ──────────────────► BIOS 键盘处理程序  │
└─────────────────────────────────────────────────────────┘

                    ↓ TSR 安装后

┌─────────────────────────────────────────────────────────┐
│  TSR 钩住中断后                                         │
│  INT 09h (键盘) ──► TSR 处理程序 ──► 原 BIOS 处理程序  │
│                     (检查热键等)     (链式调用)         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 安装流程

```
┌─────────────────────────────────────────────────────────┐
│  TSR 安装流程                                           │
│                                                         │
│  1. 用户运行 TSR 程序（如 CLOCK.COM）                   │
│     ↓                                                   │
│  2. TSR 保存原中断向量                                  │
│     INT 21h/AH=35h 获取中断向量                         │
│     ↓                                                   │
│  3. TSR 设置新中断向量（指向自己的处理程序）            │
│     INT 21h/AH=25h 设置中断向量                         │
│     ↓                                                   │
│  4. TSR 调用"终止并驻留"                                │
│     INT 21h/AH=31h                                      │
│     ↓                                                   │
│  5. DOS 释放非驻留部分，保留驻留部分                    │
│     返回命令行                                          │
│     ↓                                                   │
│  6. TSR 在内存中等待，通过中断被动激活                  │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 驻留机制详解

### 3.1 正常程序退出 vs TSR 退出

```
┌─────────────────────────────────────────────────────────────────┐
│  正常程序退出 (INT 21h, AH=4Ch)                                 │
│                                                                 │
│  内存布局:                                                       │
│  ┌──────────┐                    ┌──────────┐                   │
│  │  DOS     │                    │  DOS     │                   │
│  ├──────────┤                    ├──────────┤                   │
│  │ 程序代码 │  ──── 退出 ────►  │ (已释放) │ ◄─ 可分配给新程序 │
│  │ 程序数据 │                    │          │                   │
│  ├──────────┤                    ├──────────┤                   │
│  │  空闲    │                    │  空闲    │                   │
│  └──────────┘                    └──────────┘                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  TSR 退出 (INT 21h, AH=31h)                                     │
│                                                                 │
│  内存布局:                                                       │
│  ┌──────────┐                    ┌──────────┐                   │
│  │  DOS     │                    │  DOS     │                   │
│  ├──────────┤                    ├──────────┤                   │
│  │ 程序代码 │  ──── TSR ────►   │ 驻留部分 │ ◄─ 永久保留！     │
│  │ 程序数据 │                    ├──────────┤                   │
│  ├──────────┤                    │ (已释放) │ ◄─ 非驻留部分释放 │
│  │  空闲    │                    │  空闲    │                   │
│  └──────────┘                    └──────────┘                   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 关键系统调用

```asm
; INT 21h, AH=31h - 终止并驻留
mov ax, 3100h           ; AH=31h, AL=返回码(通常为0)
mov dx, 驻留段落数       ; 每段落 = 16 字节
int 21h
```

**参数说明**：
- `DX` = 要保留的内存大小（以 **段落** 为单位，1 段落 = 16 字节）
- DOS 会释放 DX 指定大小之后的内存，但保留之前的

### 3.3 驻留大小的计算

```asm
; COM 文件从偏移 100h (256 字节) 开始
org 100h

start:
    ; ... 安装代码 ...
    
    ; 终止并驻留
    mov ax, 3100h
    mov dx, (resident_end - start + 256 + 15) / 16
    ;        ├─────────────┬────────────────┤
    ;        │             │                │
    ;        │             │                └─ 除以 16 转换为段落
    ;        │             └─ +256 是 PSP (程序段前缀)
    ;        └─ resident_end - start = 驻留代码大小
    int 21h

; === 驻留部分 ===
new_int09:
    ; 中断处理程序...
    jmp far [cs:old_int09]

old_int09_off  dw 0
old_int09_seg  dw 0

resident_end:           ; 驻留部分结束标记

; === 非驻留部分（会被释放）===
install_msg:
    db 'TSR Installed!', 13, 10, '$'
```

### 3.4 内存布局详解

```
DOS 程序内存布局 (COM 文件):

地址           内容
────────────────────────────────────
0000h         ┌──────────────────────┐
              │  PSP (程序段前缀)     │  256 字节
              │  - 命令行参数         │
              │  - 文件句柄表         │
              │  - 环境段指针         │
0100h         ├──────────────────────┤ ◄─ org 100h (程序入口)
              │  程序代码             │
              │  - 安装代码           │
              │  - 中断处理程序       │  ← 驻留部分
              │  - 驻留数据           │
              ├──────────────────────┤ ◄─ resident_end
              │  非驻留代码           │
              │  - 初始化消息         │  ← 释放部分
              │  - 一次性数据         │
              └──────────────────────┘
                    ↓
              内存顶部（可用内存）
```

### 3.5 为什么能"驻留"？

关键在于 **DOS 的内存管理是基于链表的简单模型**：

```
┌────────────────────────────────────────────────────────────────┐
│  DOS 内存控制块 (MCB) 链表                                     │
│                                                                │
│  MCB1 ──► MCB2 ──► MCB3 ──► ... ──► 最后一个MCB               │
│   │        │        │                   │                      │
│   ▼        ▼        ▼                   ▼                      │
│  DOS    程序A     TSR程序              空闲                    │
│  内核   (运行中)  (驻留)               内存                    │
│                                                                │
│  INT 21h/AH=31h 做的事情:                                      │
│  1. 修改 TSR 程序的 MCB，标记为"驻留"                          │
│  2. 调整 MCB 大小为 DX 指定的段落数                            │
│  3. 将多余内存标记为"空闲"                                     │
│  4. 返回到 DOS 命令行（而不是父进程）                          │
└────────────────────────────────────────────────────────────────┘
```

### 3.6 驻留后如何被激活？

TSR 本身不会主动运行，它通过 **中断钩子** 被动激活：

```
┌─────────────────────────────────────────────────────────────────┐
│  时间线                                                         │
│                                                                 │
│  t=0   TSR 安装，钩住 INT 09h (键盘)                           │
│        └─► 返回 DOS 命令行                                      │
│                                                                 │
│  t=1   用户运行其他程序                                         │
│        └─► TSR 静静地待在内存中...                              │
│                                                                 │
│  t=2   用户按下键盘                                             │
│        └─► 硬件触发 INT 09h                                     │
│            └─► CPU 执行 TSR 的中断处理程序！                    │
│                └─► TSR 检查是否是热键                           │
│                    └─► 链式调用原中断处理程序                   │
│                                                                 │
│  t=3   继续执行用户程序...                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. TSR 常钩住的中断

| 中断 | 用途 | 典型应用 |
|------|------|----------|
| **INT 08h** | 硬件定时器（18.2次/秒） | 后台任务、时钟显示 |
| **INT 09h** | 键盘硬件中断 | 热键激活、键盘宏 |
| **INT 1Ch** | 用户定时器（由 INT 08h 调用） | 定时任务 |
| **INT 21h** | DOS 服务 | 扩展 DOS 功能、文件监控 |
| **INT 13h** | 磁盘服务 | 磁盘工具、加密 |
| **INT 10h** | 视频服务 | 屏幕捕获、字体替换 |
| **INT 28h** | DOS 空闲中断 | 安全执行 DOS 调用 |

### INT 28h 的特殊用途

DOS 是 **不可重入** 的，如果 TSR 在处理中断时直接调用 DOS 服务（INT 21h），可能导致死机。

INT 28h 是 DOS 在等待键盘输入时触发的"空闲"中断，此时调用 DOS 服务是安全的。

---

## 5. 著名的 TSR 程序

| 程序 | 开发商 | 功能 | 钩住的中断 |
|------|--------|------|------------|
| **SideKick** | Borland | 记事本、计算器、日历、拨号器 | INT 09h (热键 Ctrl+Alt) |
| **DOSKEY** | Microsoft | 命令行历史、宏 | INT 21h |
| **MOUSE.COM** | Microsoft/Logitech | 鼠标驱动 | INT 33h, INT 10h |
| **PRINT.COM** | Microsoft | 后台打印 | INT 08h, INT 17h |
| **Norton Guides** | Symantec | 弹出式帮助系统 | INT 09h |
| **SMARTDRV** | Microsoft | 磁盘缓存 | INT 13h |
| **中文输入法** | 各厂商 | 汉字输入（如 UCDOS） | INT 09h, INT 10h |
| **病毒扫描器** | 各厂商 | 实时文件监控 | INT 21h, INT 13h |

### SideKick - 最著名的 TSR

SideKick（1984年，Borland）是最著名的 TSR 程序，被认为是"killer app"：

- 按 Ctrl+Alt 热键弹出
- 包含记事本、计算器、日历、拨号器
- 仅占用约 64KB 内存
- 定义了 TSR 的标准模式

### 鼠标驱动 MOUSE.COM

鼠标在 DOS 时代不是标准设备，需要驱动程序：

- 钩住 INT 33h（鼠标服务）
- 钩住 INT 10h（视频服务，用于绘制光标）
- 后台持续运行，响应鼠标事件

---

## 6. TSR 安装代码示例

### 6.1 完整的键盘热键 TSR

```asm
; TSR 安装示例 - 钩住键盘中断
org 100h                    ; COM 文件格式

start:
    ; 保存原中断向量
    mov ax, 3509h           ; AH=35h 获取中断向量，AL=09h 键盘中断
    int 21h
    mov [old_int09_off], bx
    mov [old_int09_seg], es
    
    ; 设置新中断向量
    mov ax, 2509h           ; AH=25h 设置中断向量
    mov dx, new_int09       ; DS:DX = 新处理程序地址
    int 21h
    
    ; 显示安装消息
    mov ah, 09h
    mov dx, install_msg
    int 21h
    
    ; 终止并驻留
    mov ax, 3100h           ; AH=31h 终止并驻留
    mov dx, (end_resident - start + 256 + 15) / 16  ; 驻留段落数
    int 21h

; ============ 驻留部分开始 ============

; 新的键盘中断处理程序
new_int09:
    push ax
    in al, 60h              ; 读取扫描码
    cmp al, 3Bh             ; F1 键？
    jne .chain
    
    ; 检测到热键，执行 TSR 功能
    ; （这里可以添加自定义功能）
    
.chain:
    pop ax
    jmp far [cs:old_int09]  ; 链式调用原处理程序

old_int09_off  dw 0
old_int09_seg  dw 0

end_resident:               ; 驻留部分结束标记

; ============ 非驻留部分（安装后释放）============

install_msg:
    db 'TSR Installed! Press F1 to activate.', 13, 10, '$'
```

### 6.2 定时器 TSR（在屏幕显示时钟）

```asm
; 时钟 TSR - 钩住 INT 1Ch
org 100h

start:
    ; 获取原 INT 1Ch
    mov ax, 351Ch
    int 21h
    mov [old_int1c_off], bx
    mov [old_int1c_seg], es
    
    ; 设置新 INT 1Ch
    mov ax, 251Ch
    mov dx, new_int1c
    int 21h
    
    ; 终止并驻留
    mov ax, 3100h
    mov dx, (end_resident - start + 256 + 15) / 16
    int 21h

new_int1c:
    push ax
    push bx
    push es
    
    ; 计数器：每 18 次更新（约1秒）
    inc byte [cs:tick_count]
    cmp byte [cs:tick_count], 18
    jb .skip
    mov byte [cs:tick_count], 0
    
    ; 在屏幕右上角显示时间
    mov ax, 0B800h          ; 视频内存段
    mov es, ax
    mov bx, 158             ; 第0行，第79列
    mov byte [es:bx], '*'   ; 简单示例：显示一个字符
    
.skip:
    pop es
    pop bx
    pop ax
    jmp far [cs:old_int1c]

old_int1c_off  dw 0
old_int1c_seg  dw 0
tick_count     db 0

end_resident:
```

---

## 7. TSR 的问题与局限

### 7.1 主要问题

| 问题 | 说明 |
|------|------|
| **内存冲突** | 多个 TSR 争夺有限的 640KB 常规内存 |
| **中断链混乱** | TSR 加载/卸载顺序不当导致系统崩溃 |
| **重入问题** | DOS 不可重入，TSR 在中断中调用 DOS 可能死机 |
| **无隔离** | 任何 TSR 都能访问其他程序的内存 |
| **卸载困难** | 中断链一旦形成，卸载中间的 TSR 非常危险 |

### 7.2 内存管理噩梦

640KB 常规内存的典型分配：

```
┌─────────────────────────────────────────────┐
│  地址        内容              大小          │
├─────────────────────────────────────────────┤
│  0x00000    DOS 内核          ~60KB         │
│  0x10000    设备驱动          ~20KB         │
│  0x15000    TSR 1 (鼠标)      ~16KB         │
│  0x19000    TSR 2 (输入法)    ~30KB         │
│  0x21000    TSR 3 (缓存)      ~64KB         │
│  0x31000    用户程序          ~300KB        │ ← 越来越少！
│  0x9FFFF    常规内存顶部                     │
└─────────────────────────────────────────────┘
```

### 7.3 中断链问题

```
正确的中断链：
  INT 09h → TSR_C → TSR_B → TSR_A → BIOS

如果直接卸载 TSR_B（不当操作）：
  INT 09h → TSR_C → ??? → 系统崩溃！
```

---

## 8. TSR 与现代操作系统对比

| 方面 | DOS TSR | 现代 OS |
|------|---------|---------|
| **驻留机制** | INT 21h/AH=31h | 守护进程/服务 |
| **激活方式** | 中断钩子 | 系统调用/IPC |
| **内存保护** | 无 | 有（虚拟内存） |
| **卸载** | 复杂且危险 | 简单安全 |
| **通信** | 共享内存/中断 | 管道/socket/消息队列 |
| **调度** | 无（抢占式只在中断时） | 真正的抢占式多任务 |

---

## 9. 实模式的任务切换本质

实模式下手动保存/恢复栈的方式，**从功能角度看确实是一种任务切换机制**：

| 任务切换的核心要素 | 实模式（软件实现） | 保护模式（硬件 TSS） |
|-------------------|-------------------|---------------------|
| 保存当前任务状态 | ✅ 软件手动保存 | ✅ CPU 自动保存到 TSS |
| 恢复下一任务状态 | ✅ 软件手动恢复 | ✅ CPU 自动从 TSS 恢复 |
| 栈切换 | ✅ 软件修改 SS:SP | ✅ CPU 自动加载 |
| 调度决策 | ✅ 软件实现 | ✅ 软件实现 |

**本质上是相同的**：都实现了"暂停任务A，运行任务B，之后能恢复任务A"的语义。

**关键区别在于保护机制**：

| 方面 | 实模式 | 保护模式 |
|------|--------|----------|
| **实现层级** | 100% 软件实现 | 硬件辅助 + 软件 |
| **内存保护** | **零保护** - 任何代码都可以访问任何内存 | 硬件强制隔离 |
| **故障隔离** | 一个任务的 bug 可以破坏整个系统 | 硬件隔离提供故障隔离 |

> **结论**：实模式有任务切换，但没有任务保护。这就是为什么 DOS 时代的"多任务"环境（如 DESQview、TSR）非常脆弱——一个行为不端的程序就可以搞崩整个系统。真正稳定的多任务操作系统需要保护模式提供的硬件隔离。

---

## 10. 演示程序

本项目提供了可运行的 TSR 演示程序，位于 `tsr_demo/` 目录：

| 文件 | 说明 |
|------|------|
| `clock_tsr.asm` | 时钟 TSR - 在屏幕右上角显示实时时钟 |
| `hotkey_tsr.asm` | 热键 TSR - 按 F12 显示弹窗 |
| `run_tsr_demo.sh` | 一键启动脚本（自动安装 DOSBox） |

### 运行方式

```bash
cd tsr_demo
./run_tsr_demo.sh
```

在 DOSBox 中：
- 输入 `CLOCK` 加载时钟 TSR
- 输入 `HOTKEY` 加载热键 TSR
- 两个 TSR 可以同时运行！

详细说明参见 [tsr_demo/README.md](tsr_demo/README.md)。

---

## 参考文献

1. **DOS Technical Reference** - IBM
2. **The Undocumented DOS** - Andrew Schulman et al.
3. **PC Interrupts** - Ralf Brown
4. **Programming the 8086/8088** - Intel

---

## 相关文档

- [DOS 引导加载程序](DOS_BOOTLOADER.md)
- [DOS 如何使用 BIOS INT 服务](DOS_BIOS_INT_USAGE.md)
- [实模式与 TSS](X86_64_TSS_AND_IST.md#20-前置背景实模式无硬件任务切换)
- [x86 CPU 运行模式](X86_CPU_MODES.md)
