# GRUB ISO 镜像 Boot 阶段加载文件分析

本文档详细说明 GRUB ISO 镜像中哪些文件会在 boot 阶段被实际加载到内存，以及它们占用的内存大小。

## 概述

**重要说明：`core.img` 不在 ISO 镜像的文件系统中！**

GRUB ISO 镜像的引导方式与磁盘安装不同：
- **磁盘安装**：`core.img` 写入磁盘的 MBR 之后或分区间隙，作为文件系统中的文件存在
- **ISO 镜像**：`core.img` 嵌入到 ISO 镜像的 **El Torito 引导扇区**中，不在文件系统里

**ISO 镜像中的文件分为两类：**
1. **必须加载的文件**：GRUB Core（core.img），嵌入在 El Torito 引导扇区中，包含引导扇区、diskboot.S、startup_raw.S 和核心 C 代码
2. **按需加载的文件**：配置文件、模块、字体等，在文件系统中（`/boot/grub/`），由 GRUB Core 在运行时根据需要动态加载

## Boot 阶段加载流程

### 阶段 1：BIOS 加载引导扇区（El Torito 引导扇区）

**文件来源**：嵌入在 ISO 镜像的 **El Torito 引导扇区**中（不在文件系统里）

**ISO 镜像引导方式（El Torito 标准）**：
- ISO 镜像使用 **El Torito** 引导规范（不是传统的 MBR）
- El Torito 引导扇区位于 ISO 镜像的特定位置（通常在前几个扇区）
- 这个引导扇区包含 `boot.S` 和 `core.img` 的嵌入数据

**加载内容**：
- **boot.S**（引导扇区代码）
  - **磁盘位置**：El Torito 引导扇区（ISO 镜像的特定扇区）
  - **内存位置**：`0x7C00`（由 El Torito 标准指定）
  - **大小**：512 字节
  - **来源**：`grub/grub-core/boot/i386/pc/boot.S`
  - **注意**：在 ISO 镜像中，这个引导扇区可能包含 `boot.S` 和 `core.img` 的混合数据

**内存占用**：512 字节（`0x7C00 - 0x7DFF`）

**关键点**：
- **ISO 镜像中没有独立的 `core.img` 文件**：它嵌入在 El Torito 引导扇区中
- **文件系统中看不到 `core.img`**：这是正常的，因为它在 ISO 镜像的引导扇区中，不在文件系统里
- **ISO 镜像文件系统**（`/boot/grub/`）只包含模块、配置文件等运行时文件

### 阶段 2：引导扇区加载 GRUB Core 第一个扇区（diskboot.S）

**文件来源**：GRUB Core 镜像（core.img）的第一个扇区

**ISO 镜像中的 core.img 位置**：
- **不在文件系统中**：`core.img` 不在 `/boot/grub/i386-pc/core.img`
- **嵌入在 El Torito 引导扇区**：`core.img` 嵌入在 ISO 镜像的引导扇区中
- **磁盘位置**：由 El Torito 引导记录或 `boot.S` 的 `kernel_sector` 字段指定

**加载内容**：
- **diskboot.S**（GRUB Core 第一个扇区）
  - **磁盘位置**：由 `kernel_sector` 字段指定（在 ISO 镜像的引导扇区中）
  - **内存位置**：`0x8000`
  - **大小**：512 字节
    - diskboot.S 代码：约 400 字节
    - 块列表数据：12 字节（在末尾）
  - **来源**：`grub/grub-core/boot/i386/pc/diskboot.S`

**内存占用**：512 字节（`0x8000 - 0x81FF`）

**关键点**：
- **ISO 镜像中的 `core.img` 位置**：嵌入在 El Torito 引导扇区中，不在文件系统里
- **文件系统中看不到 `core.img`**：这是正常的，因为它在 ISO 镜像的引导扇区中

### 阶段 3：diskboot.S 加载完整 GRUB Core

**文件来源**：GRUB Core 镜像（core.img）的剩余部分

**ISO 镜像中的 core.img 位置**：
- **不在文件系统中**：`core.img` 不在 `/boot/grub/i386-pc/core.img`
- **嵌入在 El Torito 引导扇区**：`core.img` 嵌入在 ISO 镜像的引导扇区中
- **通过块列表读取**：`diskboot.S` 使用块列表读取 `core.img` 的剩余部分

**加载内容**：
- **startup_raw.S**（实模式入口代码）
  - **内存位置**：`0x8200`
  - **大小**：约 3.5KB（取决于编译配置）
  - **来源**：`grub/grub-core/boot/i386/pc/startup_raw.S`

- **GRUB Core C 代码**（核心功能，压缩状态）
  - **内存位置**：`0x9000+`（压缩状态，在 core.img 中）
  - **大小**：约 16KB - 24KB（压缩后，取决于编译配置）
  - **包含**：
    - `grub_main()`：主入口函数
    - 文件系统驱动框架
    - 磁盘驱动框架
    - 内存管理
    - 模块加载器
    - 等等
  - **来源**：
    - `grub/grub-core/kern/main.c`
    - `grub/grub-core/kern/disk.c`
    - `grub/grub-core/kern/file.c`
    - `grub/grub-core/kern/fs.c`
    - 等等

**内存占用**：约 8KB - 32KB（`0x8000 - 0xCFFF`，压缩状态，取决于 GRUB 配置）

**关键点**：
- GRUB Core 的大小取决于编译时的配置
- 最小配置：约 8KB
- 标准配置：约 20KB - 32KB
- 包含所有模块的配置：可能更大（但通常不会全部嵌入 core.img）

### 阶段 3.5：GRUB Core 解压（如果使用 LZMA 压缩）或直接跳转（如果未压缩）

**说明**：根据 GRUB 编译配置，GRUB Core 可能使用 LZMA 压缩，也可能未压缩。

**两种情况：**

#### 情况 1：使用 LZMA 压缩

**解压流程**（`startup_raw.S:332-356`）：
1. **切换到保护模式**：调用 `real_to_prot()` 切换到保护模式
2. **启用 A20 地址线**：调用 `grub_gate_a20` 启用 A20（访问 1MB 以上内存）
3. **解压 GRUB Core**：
   - **解压目标地址**：`GRUB_MEMORY_MACHINE_DECOMPRESSION_ADDR = 0x100000`（1MB）
   - **解压源**：`LOCAL(decompressor_end)`（压缩的 GRUB Core 代码）
   - **解压大小**：`LOCAL(uncompressed_size)`（解压后的大小）
   - **解压函数**：`_LzmaDecodeA`
   - **`%esi` 指向**：解压后的代码入口点（`0x100000`）
4. **跳转到解压后的代码**：`jmp *%esi`

**内存布局（LZMA 压缩）**：
  - 压缩的 GRUB Core：`0x8000 - 0xCFFF`（前 1MB，实模式可访问）
  - 解压后的 GRUB Core：`0x100000+`（1MB 以上，保护模式可访问）

#### 情况 2：不使用 LZMA 压缩

**处理流程**（`startup_raw.S:332-356`）：
1. **切换到保护模式**：调用 `real_to_prot()` 切换到保护模式
2. **启用 A20 地址线**：调用 `grub_gate_a20` 启用 A20（访问 1MB 以上内存）
3. **不需要解压**：
   - GRUB Core 代码没有被压缩，直接在前 1MB 中（`0x8000+`）
   - **`%esi` 指向**：未压缩的代码入口点（通常在 `LOCAL(decompressor_end)` 之后，或代码已经在 `0x8000+` 的位置）
4. **直接跳转到代码入口点**：`jmp *%esi`

**内存布局（未压缩）**：
  - GRUB Core 代码：`0x8000+`（前 1MB，实模式可访问，保护模式也可访问）
  - **不需要解压**：代码已经在正确的位置

**关键点对比：**

| 特性 | LZMA 压缩 | 未压缩 |
|------|----------|--------|
| **代码状态** | 压缩状态在 `0x8000+` | 未压缩，直接在 `0x8000+` |
| **是否需要解压** | ✅ 需要解压到 `0x100000` | ❌ 不需要解压 |
| **最终代码位置** | `0x100000+`（1MB 以上） | `0x8000+`（前 1MB） |
| **`%esi` 指向** | 解压后的代码入口点（`0x100000`） | 未压缩的代码入口点（`0x8000+`） |
| **解压时机** | 在 `startup_raw.S` 中，**此时还没有加载任何模块** | 不需要解压 |
| **模块加载** | 模块是在 `grub_main()` 之后才动态加载的，不在解压流程中 | 模块是在 `grub_main()` 之后才动态加载的 |

**关于压缩的常见问题：**

1. **前 1MB 够用吗？**
   - **如果使用 LZMA 压缩**：✅ 够用
     - 压缩后的 GRUB Core：约 8KB - 32KB（在 `0x8000 - 0xCFFF`）
     - 前 1MB 有约 640KB 可用空间，足够容纳压缩的代码
   - **如果不使用 LZMA 压缩**：⚠️ 可能不够用
     - 未压缩的 GRUB Core：约 20KB - 100KB 或更大（取决于配置）
     - 如果 GRUB Core 很大（> 100KB），前 1MB 可能不够用
     - **因此，默认情况下 GRUB 使用 LZMA 压缩**

2. **什么情况下没有压缩？**
   - **编译时禁用 LZMA**：使用 `--disable-liblzma` 配置选项
   - **系统没有 LZMA 库**：如果编译时检测不到 LZMA 库，可能不使用压缩
   - **嵌入式系统**：某些嵌入式系统可能不使用压缩以简化启动流程
   - **调试目的**：开发时可能禁用压缩以便调试

3. **是不是默认都是压缩-解压的流程？**
   - **是的，默认情况下 GRUB 使用 LZMA 压缩**
   - **原因**：
     - GRUB Core 未压缩时可能很大（几十 KB 到几百 KB）
     - 前 1MB 空间有限（约 640KB 可用）
     - 使用压缩可以：
       - 减小 core.img 的大小（压缩后约 8KB - 32KB）
       - 在前 1MB 中容纳更多代码
       - 解压到 1MB 以上，避免前 1MB 空间不足
   - **压缩流程**：
     - 编译时：GRUB Core 被 LZMA 压缩，嵌入到 core.img
     - 启动时：`startup_raw.S` 解压到 `0x100000`（1MB）
     - 解压后：代码在 1MB 以上，有足够的空间运行

**实际大小示例：**

| 配置 | 未压缩大小 | 压缩后大小（LZMA） | 压缩率 |
|------|-----------|------------------|--------|
| **最小配置** | 约 20KB | 约 8KB | ~40% |
| **标准配置** | 约 50KB - 100KB | 约 20KB - 32KB | ~30-40% |
| **完整配置** | 约 100KB - 200KB | 约 32KB - 64KB | ~30-40% |

**结论：**
- **默认使用 LZMA 压缩**：这是 GRUB 的标准配置
- **前 1MB 通常不够未压缩的代码**：如果 GRUB Core 很大（> 100KB），前 1MB 可能不够用
- **压缩-解压流程是默认的**：大多数 GRUB 安装都使用这个流程

### 阶段 4：解压后的 GRUB Core 初始化

**解压后的代码入口点**（通常是 `grub_stub_init` 或类似的初始化函数）：
- **内存位置**：`0x100000+`（解压后的代码）
- **运行模式**：保护模式
- **功能**：
  - 初始化 GRUB 核心功能（内存管理、设备驱动等）
  - 调用 `grub_main()`

**关键点**：
- **解压后的代码位置**：`0x100000`（1MB）开始
- **此时还没有加载模块**：模块是在 `grub_main()` 之后才动态加载的

### 阶段 5：GRUB Core 运行时动态加载的文件

**说明**：这些文件不是一次性全部加载，而是由 GRUB Core 在运行时（`grub_main()` 之后）根据需要动态加载。

#### 4.1 配置文件

**文件**：`boot/grub/grub.cfg`

**加载时机**：GRUB Core 初始化后，`grub_main()` 解析配置文件

**加载方式**：
- 通过文件系统驱动读取（如果已加载文件系统模块）
- 或通过 INT 13h 直接读取（如果使用 BIOS 磁盘访问）

**内存占用**：
- **读取到内存**：配置文件内容（通常几 KB 到几十 KB）
- **解析后**：配置数据结构（通常几 KB）
- **总计**：约 10KB - 50KB（取决于配置文件大小）

**关键点**：
- 配置文件不是一次性全部加载到固定内存地址
- 而是按需读取和解析
- 解析后的数据结构存储在 GRUB Core 的内存区域

#### 4.2 模块文件（.mod）

**文件来源**：`boot/grub/i386-pc/*.mod`

**加载时机**：根据 `grub.cfg` 配置和运行时需要动态加载

**必须加载的模块**（通常）：
- **文件系统驱动模块**：
  - `ext2.mod`：EXT2/EXT3/EXT4 文件系统支持
  - `iso9660.mod`：ISO 9660 文件系统支持（用于 ISO 镜像）
  - `fat.mod`：FAT 文件系统支持
  - 等等

- **磁盘驱动模块**：
  - `biosdisk.mod`：BIOS 磁盘访问（通常已嵌入 core.img）
  - `ata.mod`：ATA 磁盘驱动
  - 等等

- **其他功能模块**：
  - `normal.mod`：正常模式（交互式菜单）
  - `linux.mod`：Linux 内核加载器
  - `multiboot.mod`：Multiboot 支持
  - 等等

**可选加载的模块**（根据配置）：
- `gfxterm.mod`：图形终端支持
- `gfxmenu.mod`：图形菜单支持
- `vbe.mod`：VBE 视频模式支持
- `png.mod`：PNG 图像支持
- `jpeg.mod`：JPEG 图像支持
- 等等

**内存占用**：
- **每个模块**：通常 5KB - 50KB（取决于模块功能）
- **必须加载的模块**：约 50KB - 150KB
- **可选模块**：根据配置，可能 0KB - 几百 KB

**加载位置**：
- 模块加载到保护模式下的内存（1MB 以上）
- 具体地址由 GRUB Core 的内存管理器分配

#### 4.3 字体文件

**文件**：`boot/grub/fonts/unicode.pf2`

**加载时机**：如果需要显示图形菜单或 Unicode 字符

**内存占用**：
- **字体文件大小**：通常 50KB - 200KB（取决于字体）
- **加载到内存**：整个字体文件或部分字符集

**加载位置**：保护模式下的内存（1MB 以上）

#### 4.4 主题文件（如果使用）

**文件**：`boot/grub/themes/*/theme.txt` 和相关资源

**加载时机**：如果配置了图形主题

**内存占用**：
- **主题文件**：通常 10KB - 100KB
- **图像资源**：可能几 MB（如果使用背景图像）

## 内存占用总结

### 为什么需要切换到保护模式？

**关键问题**：如果总内存占用可能达到几 MB，为什么 GRUB Core 初始部分要加载到前 1MB？是否需要保护模式？

**答案**：

1. **实模式限制**：
   - 实模式只能访问前 1MB（`0x000000 - 0xFFFFF`）
   - 引导扇区和 GRUB Core 初始部分必须在实模式下加载（因为此时还在实模式）
   - 前 1MB 中可用空间有限（约 640KB 常规 RAM）

2. **GRUB Core 初始部分必须在前 1MB**：
   - **boot.S**（引导扇区）：`0x7C00`（512 字节）
   - **diskboot.S**：`0x8000`（512 字节）
   - **startup_raw.S**：`0x8200`（约 3.5KB）
   - **GRUB Core C 代码**：`0x9000+`（约 16KB - 24KB）
   - **总计**：约 30KB - 50KB，在前 1MB 范围内

3. **切换到保护模式后**：
   - **startup_raw.S** 会调用 `real_to_prot()` 切换到保护模式
   - 切换到保护模式后，可以访问完整的 4GB 地址空间
   - **动态加载的文件**（模块、字体、配置文件等）加载到 1MB 以上的内存
   - 这样可以避免前 1MB 空间不足的问题

4. **内存布局设计**：
   ```
   实模式阶段（前 1MB）：
   0x000000 - 0x09FFFF：常规 RAM（640KB）
   ├─ 0x7C00 - 0x7DFF：引导扇区（512 字节）
   ├─ 0x8000 - 0xCFFF：GRUB Core 压缩状态（约 30KB - 50KB）
   └─ 其他：BIOS 数据区、栈等
   
   保护模式阶段（1MB 以上）：
   0x100000 (1MB) - 0xFFFFFFFF (4GB)：
   ├─ GRUB Core 解压后（如果使用 LZMA 压缩）：约 20KB - 50KB
   │  └─ 注意：解压流程不使用模块，模块在 grub_main() 之后才加载
   ├─ Linux 内核镜像（加载时覆盖 GRUB Core）：几 MB 到几十 MB
   │  └─ 内核自己会解压到更高地址（通常 0x1000000 或更高）
   ├─ 配置文件（grub.cfg）：约 10KB - 50KB（grub_main() 之后加载）
   ├─ 模块文件（.mod）：约 50KB - 几 MB（grub_main() 之后动态加载）
   ├─ 字体文件（.pf2）：约 50KB - 2.4MB（grub_main() 之后动态加载）
   └─ 主题文件：约 10KB - 几 MB（grub_main() 之后动态加载）
   ```

5. **实际内存占用示例**（基于 ISO 镜像大小）：
   - **i386-pc 目录**：2.1MB（所有模块文件）
   - **fonts 目录**：2.4MB（字体文件）
   - **如果全部加载**：可能超过 4MB
   - **但实际不会全部加载**：只加载需要的模块和字体

### 实模式阶段（前 1MB）

| 组件 | 内存地址 | 大小 | 说明 |
|------|---------|------|------|
| **引导扇区（boot.S）** | `0x7C00 - 0x7DFF` | 512 字节 | BIOS 加载 |
| **diskboot.S** | `0x8000 - 0x81FF` | 512 字节 | boot.S 加载 |
| **startup_raw.S** | `0x8200 - 0x8FFF` | 约 3.5KB | diskboot.S 加载 |
| **GRUB Core C 代码** | `0x9000 - 0xCFFF` | 约 16KB - 24KB | diskboot.S 加载 |
| **临时缓冲区** | `0x7000 - 0x7FFF` | 4KB | 读取扇区时使用 |
| **栈空间** | `0x2000 - 0x7BFF` | 约 23KB | GRUB Core 栈 |

**实模式阶段总计**：约 30KB - 50KB（在前 1MB 范围内）

**关键点**：
- 这些组件必须在实模式下加载（因为此时还在实模式）
- 前 1MB 有足够的空间容纳这些组件（约 640KB 可用）
- 切换到保护模式后，这些代码仍然可以执行（保护模式兼容实模式代码）

### 保护模式阶段（1MB 以上）

**切换时机**：`startup_raw.S` 执行后，调用 `real_to_prot()` 切换到保护模式

**切换后**：可以访问完整的 4GB 地址空间（`0x00000000 - 0xFFFFFFFF`）

| 组件 | 内存地址 | 大小 | 说明 |
|------|---------|------|------|
| **配置文件（grub.cfg）** | 动态分配（1MB+） | 约 10KB - 50KB | 按需加载 |
| **必须加载的模块** | 动态分配（1MB+） | 约 50KB - 150KB | 按需加载 |
| **可选模块** | 动态分配（1MB+） | 0KB - 几 MB | 按需加载 |
| **字体文件** | 动态分配（1MB+） | 约 50KB - 2.4MB | 按需加载（根据 ISO 镜像） |
| **主题文件** | 动态分配（1MB+） | 约 10KB - 几 MB | 按需加载 |

**保护模式阶段总计**：约 120KB - 几 MB（取决于配置和加载的模块）

**关键点**：
- 这些文件在保护模式下动态加载到 1MB 以上的内存
- 可以访问完整的 4GB 地址空间，不受前 1MB 限制
- 根据 ISO 镜像大小（i386-pc: 2.1MB, fonts: 2.4MB），如果全部加载可能超过 4MB
- 但实际只加载需要的模块和字体，不会全部加载

### 总内存占用

**最小配置**（无图形界面，最小模块集）：
- 实模式：约 30KB
- 保护模式：约 120KB
- **总计**：约 150KB

**标准配置**（图形菜单，常用模块）：
- 实模式：约 50KB
- 保护模式：约 300KB - 500KB
- **总计**：约 350KB - 550KB

**完整配置**（所有功能，所有模块）：
- 实模式：约 50KB（前 1MB 范围内）
- 保护模式：约 1MB - 几 MB（1MB 以上，动态分配）
- **总计**：约 1MB - 几 MB

**基于 ISO 镜像的实际大小**：
- **i386-pc 目录**：2.1MB（所有模块文件）
- **fonts 目录**：2.4MB（字体文件）
- **如果全部加载**：可能超过 4MB
- **实际加载**：只加载需要的模块和字体，通常 1MB - 3MB

**关键点**：
- GRUB Core 初始部分（约 50KB）必须在前 1MB（实模式）
- 动态加载的文件（模块、字体等）在保护模式下加载到 1MB 以上
- 保护模式可以访问完整的 4GB 地址空间，不受前 1MB 限制
- 即使 ISO 镜像中有几 MB 的文件，也不会全部加载到内存

## 关键文件对应关系

### ISO 镜像中的文件 → 内存加载

**重要说明：`core.img` 不在 ISO 镜像的文件系统中！**

| 组件 | ISO 镜像位置 | 加载阶段 | 内存位置 | 大小 |
|------|------------|---------|---------|------|
| **boot.S** | El Torito 引导扇区（不在文件系统） | 阶段 1 | `0x7C00` | 512 字节 |
| **core.img（部分）** | El Torito 引导扇区（不在文件系统） | 阶段 2 | `0x8000` | 512 字节 |
| **core.img（剩余，压缩）** | El Torito 引导扇区（不在文件系统） | 阶段 3 | `0x8200+` | 约 8KB - 32KB（压缩） |
| **core.img（解压后）** | 内存中（解压后） | 阶段 3.5 | `0x100000+` | 约 20KB - 50KB（解压后） |
| **grub.cfg** | `/boot/grub/grub.cfg`（文件系统） | 阶段 4+ | 动态分配（1MB+） | 约 10KB - 50KB |
| **模块文件（.mod）** | `/boot/grub/i386-pc/*.mod`（文件系统） | 阶段 4+ | 动态分配（1MB+） | 按需加载 |
| **字体文件（.pf2）** | `/boot/grub/fonts/*.pf2`（文件系统） | 阶段 4+ | 动态分配（1MB+） | 按需加载 |

**关键点**：
- **`core.img` 不在文件系统中**：它嵌入在 ISO 镜像的 El Torito 引导扇区中
- **文件系统中看不到 `core.img`**：这是正常的，因为它在 ISO 镜像的引导扇区中
- **`eltorito.img` 文件**：在 `/boot/grub/i386-pc/eltorito.img` 中，这是用于生成 ISO 镜像的模板，不是运行时加载的文件
- **如何查看 `core.img`**：
  - 在 ISO 镜像的文件系统中：**看不到**（因为它在引导扇区中）
  - 在磁盘安装中：可以在 `/boot/grub/i386-pc/core.img` 看到
  - 在 ISO 镜像的原始二进制中：可以通过 `hexdump` 或 `dd` 查看 ISO 镜像的前几个扇区
| `boot/grub/grub.cfg` | 阶段 5 | 动态分配（1MB+） | 约 10KB - 50KB |
| `boot/grub/i386-pc/*.mod` | 阶段 5 | 动态分配（1MB+） | 按需，每个 5KB - 50KB |
| `boot/grub/fonts/unicode.pf2` | 阶段 5 | 动态分配（1MB+） | 约 50KB - 200KB |

### 源代码文件 → 编译后的二进制

| 源代码文件 | 编译后位置 | 内存位置（压缩） | 内存位置（解压后） | 说明 |
|-----------|-----------|----------------|-----------------|------|
| `grub/grub-core/boot/i386/pc/boot.S` | MBR（扇区 0）或 El Torito 引导扇区 | `0x7C00` | `0x7C00` | 引导扇区 |
| `grub/grub-core/boot/i386/pc/diskboot.S` | `core.img` 第一个扇区（磁盘：MBR 后，ISO：El Torito 引导扇区） | `0x8000` | `0x8000` | GRUB Core 第一个扇区 |
| `grub/grub-core/boot/i386/pc/startup_raw.S` | `core.img`（磁盘：MBR 后，ISO：El Torito 引导扇区） | `0x8200` | `0x100000+` | 实模式入口，解压后跳转到 1MB |
| `grub/grub-core/kern/main.c` 等 | `core.img`（压缩，磁盘：MBR 后，ISO：El Torito 引导扇区） | `0x9000+`（压缩） | `0x100000+`（解压后） | C 代码，解压到 1MB |

**关键点**：
- **磁盘安装**：`core.img` 写入磁盘的 MBR 之后或分区间隙，可以在 `/boot/grub/i386-pc/core.img` 看到
- **ISO 镜像**：`core.img` 嵌入在 ISO 镜像的 El Torito 引导扇区中，**不在文件系统里**，所以看不到
- **`eltorito.img`**：在 `/boot/grub/i386-pc/eltorito.img` 中，这是用于生成 ISO 镜像的模板，不是运行时加载的文件

## 与文档的一致性检查

### BOOT_FLOW.md

✅ **一致**：
- 引导扇区加载地址：`0x7C00`（512 字节）
- diskboot.S 加载地址：`0x8000`（512 字节）
- startup_raw.S 加载地址：`0x8200`
- GRUB Core 大小：约 8KB - 32KB

⚠️ **需要补充**：
- 配置文件（grub.cfg）的加载时机和内存占用
- 模块文件的动态加载机制
- 字体文件的加载（如果使用图形界面）

### BIOS_MEMORY_LAYOUT.md

✅ **一致**：
- GRUB Core 加载位置：`0x8000 - 0xCFFF`（约 20KB）
- 实模式内存使用：约 10KB - 20KB

⚠️ **需要补充**：
- 保护模式下的内存使用（配置文件、模块、字体等）
- 动态加载文件的内存占用

## 总结

1. **必须加载的文件**：
   - boot.S（引导扇区）：512 字节，`0x7C00`
   - core.img（GRUB Core）：约 8KB - 32KB，`0x8000+`

2. **按需加载的文件**：
   - grub.cfg：约 10KB - 50KB
   - 模块文件：按需，每个 5KB - 50KB
   - 字体文件：约 50KB - 200KB（如果使用）

3. **总内存占用**：
   - 最小配置：约 150KB
   - 标准配置：约 350KB - 550KB
   - 完整配置：约 1MB - 几 MB

4. **文档一致性**：
   - 基本一致，但需要补充动态加载文件的说明
