# SeaBIOS 如何加载 boot.bin 到 0x7C00

本文档详细说明 SeaBIOS 如何将引导扇区（boot.bin）从磁盘加载到内存地址 0x7C00 的完整过程。

## 概述

```
磁盘扇区 0 (boot.bin, 512 字节)
    ↓ [SeaBIOS INT 13h 读取]
内存地址 0x7C00 - 0x7DFF (512 字节)
    ↓ [BIOS 跳转执行]
CS:IP = 0x0000:0x7C00
```

**关键点：**
- 这是一个**从磁盘到内存的拷贝过程**（不是映射）
- 使用 **INT 13h** 磁盘服务读取扇区
- 目标地址固定为 **0x7C00**（这是 x86 架构的标准约定）

---

## 完整加载流程

### 1. 引导序列启动

SeaBIOS 完成初始化后，调用 INT 19h 引导加载服务：

```c
// SeaBIOS 源代码：src/post.c
void startBoot(void)
{
    // ... 初始化代码 ...
    call16_int(0x19, &br);  // 调用 INT 19h 引导加载服务
}
```

### 2. INT 19h 处理程序

INT 19h 处理程序选择引导设备并调用 `boot_disk()`：

```c
// SeaBIOS 源代码：src/boot.c
void handle_19(void)
{
    BootSequence = 0;
    do_boot(0);  // 从第一个引导设备开始尝试
}
```

### 3. boot_disk() 函数 - 核心加载逻辑

这是将 boot.bin 加载到 0x7C00 的关键函数：

```c
// SeaBIOS 源代码：src/boot.c:boot_disk()
static void
boot_disk(u8 bootdrv, int checksig)
{
    u16 bootseg = 0x07c0;  // 引导扇区加载地址：段地址 0x07C0
                           // 物理地址 = 0x07C0 * 16 + 0x0000 = 0x7C00

    // 步骤 1: 准备 INT 13h 参数
    struct bregs br;
    memset(&br, 0, sizeof(br));  // 初始化所有寄存器为0，包括 BX = 0
    br.flags = F_IF;      // 允许中断
    br.dl = bootdrv;      // DL = 驱动器号（0x00 软盘，0x80 硬盘）
    br.es = bootseg;      // ES = 目标段地址（0x07C0）← 关键！
    // br.bx = 0;         // BX = 0（通过 memset 已初始化为0）
    br.ah = 2;            // AH = 0x02：读扇区功能
    br.al = 1;            // AL = 读取扇区数（1 个扇区 = 512 字节）
    br.cl = 1;            // CL = 扇区号（第 1 个扇区，CHS 模式）

    // 步骤 2: 调用 INT 13h 读取引导扇区
    call16_int(0x13, &br);  // 调用 INT 13h 磁盘服务

    // 步骤 3: 检查读取是否成功
    if (br.flags & F_CF) {  // CF（进位标志）表示错误
        printf("Boot failed: could not read the boot disk\n\n");
        return;
    }

    // 步骤 4: 验证引导扇区签名（0xAA55）
    if (checksig) {
        struct mbr_s *mbr = (void*)0;  // 在段 0x07C0 的偏移 0 处
        if (GET_FARVAR(bootseg, mbr->signature) != MBR_SIGNATURE) {
            printf("Boot failed: not a bootable disk\n\n");
            return;
        }
    }

    // 步骤 5: 跳转到引导扇区程序执行
    u16 bootip = (bootseg & 0x0fff) << 4;
    bootseg &= 0xf000;
    call_boot_entry(SEGOFF(bootseg, bootip), bootdrv);
}
```

---

## 关键代码解析

### 地址计算：为什么是 0x7C00？

```c
u16 bootseg = 0x07c0;  // 段地址
// br.bx = 0;          // 偏移地址（通过 memset 初始化为 0）

// 实模式地址计算：
物理地址 = ES × 16 + BX
        = 0x07C0 × 16 + 0x0000
        = 0x7C00 + 0x0000
        = 0x7C00
```

**段:偏移格式：** `ES:BX = 0x07C0:0x0000`  
**物理地址：** `0x7C00`  
**地址范围：** `0x7C00 - 0x7DFF`（512 字节）

### INT 13h AH=0x02 寄存器设置

| 寄存器 | 值 | 说明 |
|--------|-----|------|
| **AH** | `0x02` | 功能号：读扇区 |
| **AL** | `1` | 读取扇区数（1 个扇区 = 512 字节） |
| **DL** | `0x80` | 驱动器号（0x80 = 第一块硬盘，0x00 = 软盘） |
| **DH** | `0` | 磁头号（CHS 模式） |
| **CH** | `0` | 柱面号低8位（CHS 模式） |
| **CL** | `1` | 扇区号（第 1 个扇区，CHS 模式） |
| **ES:BX** | `0x07C0:0x0000` | **目标缓冲区地址**（物理地址 0x7C00） |

---

## INT 13h 处理程序内部实现

SeaBIOS 的 INT 13h 处理程序接收这些参数后，执行以下操作：

```c
// SeaBIOS INT 13h 处理程序（简化示例）
void int13_handler(struct bregs *br) {
    if (br->ah == 0x02) {  // 读扇区功能
        // 1. 计算目标物理地址
        u32 target_addr = (br->es << 4) + br->bx;
        // target_addr = 0x07C0 * 16 + 0x0000 = 0x7C00
        
        // 2. 从磁盘读取扇区
        u8 sector = br->cl;  // 扇区号（1，即第一个扇区）
        u8 drive = br->dl;   // 驱动器号（0x80 = 硬盘）
        
        // 3. 调用磁盘控制器读取扇区到内存
        disk_read_sector(drive, sector, target_addr, 512);
        // 将磁盘扇区0的512字节数据拷贝到内存地址0x7C00
        
        // 4. 设置返回状态
        if (成功) {
            br->flags &= ~F_CF;  // 清除进位标志（表示成功）
        } else {
            br->flags |= F_CF;   // 设置进位标志（表示失败）
        }
    }
}
```

---

## 数据拷贝过程详解

### 步骤 1: BIOS 调用 INT 13h

```
boot_disk() 函数设置参数：
├─ AH = 0x02（读扇区功能）
├─ AL = 1（读取1个扇区 = 512字节）
├─ DL = 0x80（第一块硬盘）
├─ CL = 1（扇区号，CHS模式）
└─ ES:BX = 0x07C0:0x0000（目标地址）

调用：call16_int(0x13, &br)
```

### 步骤 2: INT 13h 处理程序计算地址

```
INT 13h 处理程序：
├─ 读取 br.es = 0x07C0
├─ 读取 br.bx = 0x0000
└─ 计算物理地址 = 0x07C0 × 16 + 0x0000 = 0x7C00
```

### 步骤 3: 磁盘控制器读取扇区

```
磁盘控制器操作：
├─ 定位到扇区 0（LBA = 0）
├─ 从磁盘读取 512 字节数据
└─ 准备通过 DMA 传输到内存
```

### 步骤 4: 数据传输到内存

**传输模式：** 引导扇区通常使用 **PIO（Programmed I/O）** 模式，而不是 DMA。

**PIO 传输过程：**
```
磁盘扇区 0（512 字节）
    ↓ [磁盘控制器读取]
ATA 控制器内部缓冲区（512 字节）
    ↓ [I/O 端口 0x1F0]
CPU 寄存器（AX，每次 16 位）
    ↓ [rep insw 指令循环 256 次]
内存地址 0x7C00 - 0x7DFF（512 字节）
```

**详细过程：**
1. ATA 控制器从磁盘读取扇区 0 到内部缓冲区
2. CPU 执行 `rep insw` 指令，从 I/O 端口 0x1F0 读取数据
3. 每次读取 16 位（2 字节），重复 256 次
4. 数据被写入内存地址 0x7C00 - 0x7DFF

> **详细说明：** 关于数据传输的完整过程（PIO 和 DMA 模式），请参见 [DISK_TO_MEMORY_TRANSFER.md](DISK_TO_MEMORY_TRANSFER.md)

### 步骤 5: 验证引导扇区签名

```c
// 验证最后两个字节是否为 0xAA55
struct mbr_s *mbr = (void*)0;  // 在段 0x07C0 的偏移 0 处
if (GET_FARVAR(bootseg, mbr->signature) != MBR_SIGNATURE) {
    // 签名不匹配，引导失败
    return;
}
// 签名匹配，继续引导
```

**验证位置：**
- 文件偏移：`0x1FE - 0x1FF`（最后两个字节）
- 内存地址：`0x7DFE - 0x7DFF`
- 期望值：`0x55 0xAA`（小端序，对应值 `0xAA55`）

### 步骤 6: 跳转到引导扇区执行

```c
// 跳转到 0x0000:0x7C00 执行引导扇区代码
u16 bootip = (bootseg & 0x0fff) << 4;  // IP = 0x7C00
bootseg &= 0xf000;                      // CS = 0x0000
call_boot_entry(SEGOFF(bootseg, bootip), bootdrv);
// 跳转到 CS:IP = 0x0000:0x7C00
```

---

## 验证 boot.bin 的内容

使用 `hexdump` 查看 boot.bin 的内容：

```bash
$ hexdump -C boot.bin | head -5
00000000  b8 03 00 cd 10 be 15 7c  b4 0e ac 84 c0 74 04 cd  |.......|.....t..|
00000010  10 eb f7 eb fe 48 65 6c  6c 6f 20 66 72 6f 6d 20  |.....Hello from |
00000020  42 6f 6f 74 20 53 65 63  74 6f 72 21 00 00 00 00  |Boot Sector!....|
00000030  00 00 00 00 00 00 00 00  00 00 00 00 00 00 00 00  |................|
```

**内容解析：**

| 文件偏移 | 内存地址 | 字节序列 | 说明 |
|---------|---------|---------|------|
| 0x000 | 0x7C00 | `B8 03 00` | `mov ax, 0x0003` |
| 0x003 | 0x7C03 | `CD 10` | `int 0x10` |
| 0x005 | 0x7C05 | `BE 15 7C` | `mov si, 0x7C15` |
| 0x008 | 0x7C08 | `B4 0E` | `mov ah, 0x0E` |
| 0x00A | 0x7C0A | `AC` | `lodsb` |
| 0x00B | 0x7C0B | `84 C0` | `test al, al` |
| 0x00D | 0x7C0D | `74 04` | `je 0x7C13` |
| 0x00F | 0x7C0F | `CD 10` | `int 0x10` |
| 0x011 | 0x7C11 | `EB F7` | `jmp 0x7C0A` |
| 0x013 | 0x7C13 | `EB FE` | `jmp 0x7C13` (无限循环) |
| 0x015 | 0x7C15 | `48 65 6C 6C 6F...` | 字符串 "Hello from Boot Sector!" |
| 0x1FE | 0x7DFE | `55` | 引导签名低字节 |
| 0x1FF | 0x7DFF | `AA` | 引导签名高字节 |

**关键验证点：**

1. **文件大小：** 必须是 512 字节（0x200 字节）
2. **引导签名：** 最后两个字节必须是 `0x55 0xAA`（小端序）
3. **代码位置：** 第一条指令在文件偏移 0x000（对应内存 0x7C00）

---

## 完整流程图

```
┌─────────────────────────────────────────────────────────┐
│ SeaBIOS 初始化完成                                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ INT 19h 引导加载服务                                      │
│ - 选择引导设备（硬盘/软盘）                                │
│ - 调用 boot_disk(0x80, 1)                                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ boot_disk() 函数                                         │
│ 1. 设置 bootseg = 0x07C0                                 │
│ 2. 准备 INT 13h 参数：                                    │
│    - AH = 0x02（读扇区）                                  │
│    - AL = 1（1个扇区）                                    │
│    - DL = 0x80（硬盘）                                    │
│    - CL = 1（扇区号）                                     │
│    - ES:BX = 0x07C0:0x0000（目标地址）                    │
│ 3. 调用 call16_int(0x13, &br)                            │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ INT 13h 处理程序                                          │
│ 1. 计算物理地址：0x07C0 × 16 + 0x0000 = 0x7C00          │
│ 2. 调用磁盘控制器读取扇区 0                               │
│ 3. 通过 PIO 传输 512 字节到内存 0x7C00                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 磁盘控制器 PIO 传输                                      │
│ 磁盘扇区 0 (512 字节)                                    │
│     ↓ [I/O 端口 0x1F0]                                   │
│ CPU 执行 rep insw (256 次)                               │
│     ↓                                                    │
│ 内存地址 0x7C00 - 0x7DFF (512 字节)                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 验证引导扇区签名                                          │
│ - 检查 0x7DFE 处的值是否为 0x55                          │
│ - 检查 0x7DFF 处的值是否为 0xAA                          │
│ - 如果匹配，继续；否则失败                                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 跳转到引导扇区执行                                        │
│ call_boot_entry(SEGOFF(0x0000, 0x7C00), bootdrv)        │
│ CS:IP = 0x0000:0x7C00                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 引导扇区代码开始执行                                      │
│ 第一条指令：mov ax, 0x0003 (地址 0x7C00)                 │
└─────────────────────────────────────────────────────────┘
```

---

## 关键要点总结

1. **加载方式：** 使用 INT 13h AH=0x02（读扇区）从磁盘读取
2. **目标地址：** 固定为 0x7C00（x86 架构标准约定）
3. **地址计算：** ES:BX = 0x07C0:0x0000 → 物理地址 0x7C00
4. **数据拷贝：** 通过 PIO（或 DMA）从磁盘扇区 0 拷贝 512 字节到内存
5. **验证签名：** 检查最后两个字节是否为 0xAA55
6. **执行跳转：** 跳转到 CS:IP = 0x0000:0x7C00 执行引导代码

---

## 相关文档

- [BOOT_FLOW.md](BOOT_FLOW.md) - 完整的引导流程分析
- [BOOT_SECTOR_ANALYSIS.md](BOOT_SECTOR_ANALYSIS.md) - 引导扇区代码分析
- [DISK_TO_MEMORY_TRANSFER.md](DISK_TO_MEMORY_TRANSFER.md) - 磁盘数据拷贝到内存的详细过程（PIO/DMA）
- [verify_boot_sector.py](verify_boot_sector.py) - 验证脚本
