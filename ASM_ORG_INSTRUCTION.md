# 汇编语言中的 `org` 指令详解

## 概述

`org`（Origin）指令是汇编语言中用于设置程序起始地址的伪指令。它告诉汇编器程序在内存中的起始位置，从而正确计算标签和变量的绝对地址。

---

## 基本语法

```asm
org 地址值
```

**示例：**
```asm
org 0x7C00    ; 设置程序起始地址为 0x7C00
org 0x100     ; 设置程序起始地址为 0x100（COM 文件）
org 0x0100    ; 同上，十六进制表示
```

---

## 作用

### 1. 设置程序起始地址

`org` 指令告诉汇编器：**程序将从指定的内存地址开始执行**。

```asm
org 0x7C00
start:
    mov ax, 0x0003
    ; 汇编器知道 start 标签的地址是 0x7C00
```

### 2. 影响标签地址计算

所有标签和变量的地址都是相对于 `org` 指定的起始地址计算的。

```asm
org 0x7C00

start:          ; 地址 = 0x7C00
    mov ax, 0x0003
    
label1:         ; 地址 = 0x7C03（假设前面代码占用 3 字节）
    int 0x10
    
msg db "Hello"  ; 地址 = 0x7C05（假设前面代码占用 5 字节）
```

### 3. 影响绝对地址引用

当代码中使用标签时，汇编器会将其替换为基于 `org` 计算的绝对地址。

```asm
org 0x7C00

    mov si, msg     ; 汇编器将 msg 替换为 0x7C15（假设）
msg db "Hello", 0
```

---

## 工作原理

### 地址计算过程

**汇编器的计算步骤：**

1. **读取 `org` 指令**：记录起始地址（例如 `0x7C00`）

2. **跟踪当前位置**：
   ```
   当前位置 = org 地址 + 已生成的代码大小
   ```

3. **计算标签地址**：
   ```
   标签地址 = org 地址 + 标签的文件偏移
   ```

4. **替换标签引用**：
   - 将源代码中的标签替换为计算出的绝对地址
   - 生成相应的机器码

### 示例：boot.asm

```asm
org 0x7C00        ; 起始地址 = 0x7C00

start:            ; 地址 = 0x7C00 + 0x00 = 0x7C00
    mov ax, 0x0003    ; 3 字节，当前位置 = 0x7C03
    int 0x10          ; 2 字节，当前位置 = 0x7C05
    mov si, msg       ; 3 字节，当前位置 = 0x7C08
    ; ... 其他代码 ...
    
msg db "Hello..."     ; 地址 = 0x7C00 + 0x15 = 0x7C15
```

**编译结果：**
- `mov si, msg` → `mov si, 0x7C15`
- 机器码：`BE 15 7C`（小端序）

---

## 常见使用场景

### 1. 引导扇区程序

```asm
org 0x7C00
; BIOS 会将引导扇区加载到 0x7C00
; 必须使用 org 0x7C00 才能正确计算地址
```

**为什么需要：**
- BIOS 固定将引导扇区加载到 `0x7C00`
- 代码中的标签地址必须基于 `0x7C00` 计算
- 否则跳转和数据访问会出错

### 2. DOS COM 文件

```asm
org 0x100
; DOS COM 文件从 0x100 开始（前 256 字节是 PSP）
```

**为什么需要：**
- DOS 加载 COM 文件时，从 `0x100` 开始执行
- 前 256 字节（0x0000-0x00FF）是程序段前缀（PSP）

### 3. 可执行文件（EXE）

```asm
; EXE 文件通常不需要 org
; 链接器会处理地址重定位
```

**为什么不需要：**
- EXE 文件支持重定位
- 链接器会处理地址计算
- 但某些情况下仍可使用 `org`

---

## 重要特性

### 1. `org` 指令本身不生成代码

**关键点：**
- `org` 是**伪指令**（assembler directive），不是真正的指令
- 它不会出现在编译后的机器码中
- 它只影响汇编器的地址计算

**验证：**
```asm
org 0x7C00
start:
    mov ax, 0x0003
```

编译后的机器码：
```
B8 03 00    ; mov ax, 0x0003
```

**没有 `org` 的痕迹**，但它影响了 `start` 标签的地址计算。

### 2. 可以多次使用 `org`

```asm
org 0x7C00
    ; 代码部分
    mov ax, 0x0003
    
org 0x7C00 + 0x100
    ; 数据部分
data:
    db 0, 1, 2, 3
```

**注意：** 通常不推荐多次使用 `org`，容易出错。

### 3. 相对地址不受 `org` 影响

**相对跳转指令：**
```asm
org 0x7C00
    jz label1      ; 相对跳转，使用相对偏移量
label1:
    nop
```

**编译结果：**
- `jz label1` → `74 02`（相对偏移量）
- 相对偏移量不依赖 `org`，但计算时需要知道绝对地址

**关键理解：**
- 相对跳转存储的是**相对偏移量**（如 `0x04`）
- 但汇编器计算偏移量时，需要知道**绝对地址**
- 所以 `org` 仍然影响相对跳转的计算

---

## 常见错误

### 错误 1：忘记使用 `org`

```asm
; 错误：没有 org
start:
    mov si, msg
msg db "Hello", 0
```

**问题：**
- 汇编器假设起始地址是 `0x0000`
- `msg` 地址被计算为 `0x0015`（假设）
- 如果程序加载到 `0x7C00`，实际地址是 `0x7C15`
- 代码会访问错误的内存位置

### 错误 2：`org` 地址与实际加载地址不匹配

```asm
org 0x1000    ; 错误：假设加载到 0x1000
start:
    mov si, msg
msg db "Hello", 0
```

**问题：**
- 如果程序实际加载到 `0x7C00`
- 标签地址计算错误
- 程序无法正确执行

### 错误 3：在需要 `org` 的地方不使用

```asm
; 引导扇区程序
; 错误：没有 org 0x7C00
start:
    mov ax, 0x0003
    mov si, msg    ; msg 地址计算错误
msg db "Hello", 0
```

---

## 与其他指令的对比

### `org` vs `section`

**`org`：**
- 设置程序的起始地址
- 影响所有后续代码的地址计算
- 适用于简单程序（如引导扇区）

**`section`：**
- 定义代码段、数据段等
- 链接器会处理地址分配
- 适用于复杂程序（如 EXE 文件）

### `org` vs 链接器地址

**使用 `org`：**
- 汇编器直接计算绝对地址
- 适合固定加载地址的程序

**使用链接器：**
- 链接器处理地址重定位
- 适合可重定位的程序

---

## 实际应用示例

### 示例 1：引导扇区

```asm
org 0x7C00
bits 16

start:
    mov ax, 0x0003
    int 0x10
    mov si, msg
    mov ah, 0x0E
print_loop:
    lodsb
    test al, al
    jz halt
    int 0x10
    jmp print_loop
halt:
    jmp $

msg db "Hello from Boot Sector!", 0
times 510-($-$$) db 0
dw 0xAA55
```

**关键点：**
- `org 0x7C00` 确保所有地址基于 `0x7C00` 计算
- `mov si, msg` 会编译成 `mov si, 0x7C15`（假设）
- 相对跳转（`jz`, `jmp`）的偏移量也基于 `org` 计算

### 示例 2：DOS COM 文件

```asm
org 0x100
bits 16

start:
    mov dx, msg
    mov ah, 0x09
    int 0x21
    mov ax, 0x4C00
    int 0x21

msg db "Hello, DOS!$"
```

**关键点：**
- `org 0x100` 因为 DOS COM 文件从 `0x100` 开始
- 前 256 字节是 PSP（程序段前缀）

### 示例 3：不使用 `org`（不推荐）

```asm
; 没有 org
start:
    mov ax, 0x0003
    ; 汇编器假设起始地址是 0x0000
```

**问题：**
- 如果程序加载到非零地址，所有地址引用都会错误
- 不适用于引导扇区等固定地址程序

---

## 验证 `org` 的效果

### 方法 1：查看反汇编

```bash
objdump -D -b binary -m i8086 -M intel boot.bin
```

**输出：**
```
   5:	be 15 7c             	mov    si,0x7c15
```

可以看到 `mov si, 0x7c15`，这里的 `0x7c15` 就是基于 `org 0x7C00` 计算的。

### 方法 2：对比有无 `org`

**有 `org 0x7C00`：**
```asm
org 0x7C00
    mov si, msg
msg db "Hello", 0
```
编译结果：`BE 15 7C`（`mov si, 0x7C15`）

**无 `org`：**
```asm
    mov si, msg
msg db "Hello", 0
```
编译结果：`BE 15 00`（`mov si, 0x0015`）← 错误！

---

## 总结

### `org` 指令的核心作用

1. **设置程序起始地址**：告诉汇编器程序从哪个地址开始
2. **影响地址计算**：所有标签和变量的地址都基于 `org` 计算
3. **生成正确的机器码**：确保绝对地址引用正确

### 关键要点

- **`org` 是伪指令**：不生成机器码，只影响汇编过程
- **必须匹配实际加载地址**：`org` 指定的地址必须与实际加载地址一致
- **影响所有地址计算**：包括绝对地址和相对跳转的偏移量计算
- **编译时作用**：只在编译时影响，执行时 CPU 使用编译后的机器码

### 使用建议

1. **引导扇区**：必须使用 `org 0x7C00`
2. **DOS COM 文件**：必须使用 `org 0x100`
3. **固定地址程序**：根据实际加载地址设置 `org`
4. **可重定位程序**：通常不需要 `org`，由链接器处理

---

## 相关文档

- [ORG_0x7C00_EXPLANATION.md](ORG_0x7C00_EXPLANATION.md) - org 0x7C00 在编译后代码中的体现
- [RELATIVE_JUMP_EXECUTION.md](RELATIVE_JUMP_EXECUTION.md) - 相对跳转指令的执行过程
- [BOOT_SECTOR_ANALYSIS.md](BOOT_SECTOR_ANALYSIS.md) - 引导扇区代码手工分析指南
- [boot.asm](boot.asm) - 引导扇区源代码示例
