# TSS 与内核栈切换：GDT、成本及设计演变

本文档总结「用户态↔内核态」栈切换与 TSS 的关系、与 CPL 检查的成本对比、TSS.sp2 的用途、以及 TSS 从 per-task 到 per-CPU 的演变。

---

## 1. 切换内核栈与 GDT/TSS 的关系

切换内核栈时用到的栈指针（RSP0 或 ISTn）来自 **TSS**。要访问 TSS，必须通过 **TR → GDT 中的 TSS 描述符** 这一路径，这是 x86 的硬性规定（段选择子必须到 GDT 中索引描述符）；但**查 GDT 的代价是一次性的**，见下。

### 1.1 TSS 描述符与 TR

- **TSS 描述符在 GDT 里**：每个 CPU 有一个 TSS，对应一条 TSS 描述符，通常放在 GDT 中。`LTR` 的操作数就是指向该描述符的**选择子**。
- **TR 的可见部分与隐藏部分**：与 CS、DS 等段寄存器类似，TR 有**可见部分**（16 位选择子）和 **CPU 内部隐藏部分**（缓存，含 TSS 基址、界限、属性等）。执行 `LTR` 时，CPU 用选择子查 GDT，将 TSS 描述符中的基址、界限、属性**加载到 TR 的隐藏缓存**；**此后 CPU 访问 TSS 时直接使用该缓存，不再重新查 GDT**。
- **Linux 的用法**：**每个 CPU 一个 TSS**，**永远不切换 TR**（不在进程切换时执行 LTR）。系统初始化时每 CPU 执行一次 `LTR` 加载该 TSS 选择子；进程切换时只做一件事：**更新这个唯一 TSS 中的 RSP0**（以及 IST 若需要），使其指向新进程的内核栈。因此 TR 的隐藏缓存始终有效，中断/异常时 CPU 直接从 TR 缓存取 TSS 基址、再读 RSP0/IST，**查 GDT 不在每次中断的关键路径上**。

**何时会重新查 GDT**：只有再次执行 `LTR` 加载新的 TSS 选择子、或加载其他段寄存器等操作时，CPU 才会重新访问 GDT；日常的中断/系统调用不会触发对 GDT 的查询。

**结论**：切换内核栈一定会用到 TSS；TSS 来自「TR 指向的那条 GDT 描述符」。**查 GDT 只发生在加载 TR 时**（内核初始化时每 CPU 一次 `LTR`）；之后每次中断/异常做栈切换时，CPU 用 TR 的隐藏部分直接访问 TSS，不再查 GDT。可类比 TLB：第一次 LTR 相当于“查页表”，之后每次用 TSS 相当于“命中 TLB”。

---

## 2. 内核栈切换 vs CS.RPL 检查：成本对比

与「看 CS.RPL 做特权检查」相比，**通过 TSS 做内核栈切换是一条更贵的路径**。

### 2.1 CS.RPL / CPL 的检查（相对便宜）

- CPL 来自 **CS.RPL**；段寄存器有隐藏部分，描述符信息（含 RPL/DPL）在加载 CS 时已缓存在 CPU 内。
- 特权检查（如访问段、中断门 DPL 检查）主要用这些**已缓存字段做比较**，不必再访存。
- 成本主要是几条整数比较 / 门描述符解码，**不涉及额外到内存里读一大块结构**。

### 2.2 TSS 内核栈切换（相对昂贵）

- **必须用 TSS**：即便 TSS 基址在 TR 里已缓存，**RSP0 / ISTn 的值在 TSS 结构体里**，CPU 仍要**至少一次内存读**（读 TSS 中对应偏移）才能得到新 RSP。
- 若 TSS 不在 cache 里，会多出 **cache line 加载**。
- 切换栈后还要在**新栈上压多帧**（SS, RSP, RFLAGS, CS, RIP，可能还有错误码），即**多次写内存**。

因此这条路径 = **至少 1 次 TSS 读 + 多次栈写**，与「只比一下 CS.RPL 和 DPL」不在同一量级。

### 2.3 小结

| 操作 | 成本 |
|------|------|
| CS.RPL/CPL 的查询 | 用已缓存的段/门描述符信息，成本低。 |
| 通过 TSS 做 kernel stack 切换 | 必须读 TSS 中的 RSP0/IST，再加一系列栈上的压栈，访存更多，是更昂贵的操作。 |

这也是 **SYSCALL** 等路径会避免用 TSS 换栈，而用 **MSR + 内核里自己保存/恢复 RSP**，以减少对 TSS 的依赖。

---

## 3. TSS.RSP2（sp2）的用途

**RSP2（TSS 里的 sp2）** 在「用户态 → 内核态」时，被用来**临时存放用户态栈顶（用户 RSP）**。这是 **Linux 在 SYSCALL 入口上的软件约定**，不是 CPU 的硬性规定。

### 3.1 流程（Linux SYSCALL 入口）

1. 用户态执行 `syscall`，此时 **RSP 仍是用户栈顶**。
2. 内核入口需要切到内核栈，但尚未切换前无法往内核栈上压栈。
3. 先将当前 RSP（用户栈顶）存到 **TSS.sp2**：  
   `movq %rsp, PER_CPU_VAR(cpu_tss_rw + TSS_sp2)`
4. 再从 per-CPU 变量把内核栈加载到 RSP，完成栈切换。
5. 之后把 TSS.sp2 里保存的值拷进 **pt_regs**；返回用户态时从 pt_regs 恢复用户 RSP。

**结论**：RSP2 在 userspace 转到 kernel space 时用来**临时保存 user space 的栈顶位置**；长期保存是在 **pt_regs** 里，sp2 仅作过渡。

### 3.2 保存的值：用户空间虚拟地址

保存到 TSS.sp2（以及后续拷到 pt_regs）的，是**用户态看到的栈顶地址**，即**用户空间的虚拟地址**。

- 在用户态时，RSP 由 MMU 在当前进程的**用户页表**下解释，指向用户栈顶，属于该进程用户虚地址空间（如 64 位 Linux 下常见的 `0x7ff...`）。
- 进内核时只是把当时 RSP 的数值原样存起来，不做转换；返回用户态时再将该值写回 RSP，并恢复用户页表，因此该值在用户态下仍然有效。
- 内核不会把它当内核虚拟地址或物理地址使用；它只在**回到该用户进程**时，作为其用户栈顶的虚拟地址被恢复。

---

## 4. TSS 设计之初：per-task 与现代 per-CPU 用法

### 4.1 原始设计（286/386 硬件任务切换）

在 x86 设计之初，TSS 是按 **per-task（每个任务一个）** 设计的：

- 每个**任务**在 GDT 里有一条 **TSS 描述符**，对应一个 TSS。
- TSS 中保存该任务的**完整 CPU 状态**：通用寄存器、段寄存器、EIP、EFLAGS、CR3、栈指针（RSP0/RSP1/RSP2）等。
- 发生**任务切换**时（CALL/JMP 到 TSS、IRET 从嵌套任务返回、或通过**任务门**的中断/异常）：
  - CPU 将**当前任务**的现场写回**当前 TSS**；
  - 再从**新任务的 TSS** 加载新任务现场；
  - **TR** 更新为指向新任务的 TSS。

设计意图：**一个任务 ↔ 一个 TSS**，用 TSS 作为「任务状态块」，由硬件自动保存/恢复。

### 4.2 现代 OS（如 Linux）的用法

- 不使用**硬件任务切换**，改用**软件调度**：寄存器保存在内核栈 / `task_struct` 等，由 OS 切换页表、栈等。硬件任务切换会保存/恢复**所有**寄存器，过于笨重；软件切换只保存需要的内容，更灵活。
- 因此不再为每个进程维护一个 TSS，而是 **每个 CPU 一个全局唯一的 TSS**，且 **永远不切换 TR**，使其始终指向这个唯一的 TSS。进程切换时只做一件事：**更新该 TSS 中的 RSP0**（以及 IST 若需），使其指向新进程的内核栈。
- TSS 中不再保存「整个任务状态」，只保留架构要求的部分：**RSP0**（及 64 位下的 RSP1/RSP2）、**IST**、32 位下还有 I/O 位图等。

**结论**：TSS 在设计之初是 **per-task** 的，用于硬件任务切换；现代 OS 将其用成 **per-CPU**，仅保留为**中断/系统调用提供初始内核栈指针**这一单一职责。查 GDT 更像为兼容旧设计而必须的「初始化仪式」，并非日常运行的性能瓶颈；这是一种**适应性演化**：古老硬件机制在 OS 手中被轻量化、赋予新的用法。

### 4.3 与其他架构对比（简要）

| 架构 | 内核栈切换 | 说明 |
|------|------------|------|
| **x86-64（Linux 用法）** | 依赖 TSS 中的 RSP0（或 SYSCALL 路径用 per-CPU 变量）；中断/异常时 CPU 从 TSS 取栈。 | 需 GDT/TSS，LTR 一次后靠 TR 缓存。 |
| **ARM64** | **SP_EL0**（用户栈）与 **SP_EL1**（内核栈）独立；进入异常时 CPU 自动切到 SP_EL1，无需查表。 | 寄存器直接切换，无 TSS。 |
| **RISC-V** | **sscratch** CSR 在用户态/内核态间交换栈指针；陷入时硬件交换 sp 与 sscratch 完成栈切换。 | CSR 直接指定，软件控制灵活。 |

---

## 相关文档

- [X86_64_TSS_AND_IST.md](X86_64_TSS_AND_IST.md) — TSS/IST/TR 与栈切换细节
- [X86_MEMORY_MANAGEMENT_THEORY.md](X86_MEMORY_MANAGEMENT_THEORY.md) — CPL、DPL 与特权检查
- [X86_GDT_STRUCTURE.md](X86_GDT_STRUCTURE.md) — GDT 与描述符布局
