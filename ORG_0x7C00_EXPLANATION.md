# org 0x7C00 在编译后代码中的体现

## 关键点

**`org 0x7C00` 指令本身不会出现在编译后的二进制文件中，但它会影响标签地址的计算。**

---

## 源代码中的体现

在 `boot.asm` 中：

```asm
org 0x7C00
; ... 其他代码 ...
    mov si, msg      ; 将 msg 标签的地址加载到 SI
; ... 其他代码 ...
msg db "Hello from Boot Sector!", 0
```

---

## 编译后的体现

### 1. `mov si, msg` 指令

**源代码：**
```asm
mov si, msg
```

**编译后的机器码：**
```
BE 15 7C
```

**反汇编输出：**
```
   5:	be 15 7c             	mov    si,0x7c15
```

**关键分析：**
- `BE` = `mov si, imm16` 的操作码
- `15 7C` = 立即数 `0x7C15`（小端序：低字节在前）
  - `0x15` 是低字节
  - `0x7C` 是高字节
  - 组合：`0x7C15`

**这就是 `org 0x7C00` 的体现！**

### 2. 地址计算过程

**汇编器的计算过程：**

1. **`org 0x7C00`** 告诉汇编器：程序的起始地址是 `0x7C00`

2. **`msg` 标签的位置：**
   - 程序从 `0x7C00` 开始
   - 代码部分占用约 21 字节（0x7C00-0x7C14）
   - `msg` 标签在文件偏移 `0x15`（21 字节）
   - **`msg` 的绝对地址 = 0x7C00 + 0x15 = 0x7C15**

3. **`mov si, msg` 编译：**
   - 汇编器将 `msg` 标签替换为计算出的绝对地址 `0x7C15`
   - 生成指令：`mov si, 0x7C15`
   - 机器码：`BE 15 7C`（小端序）

---

## 验证方法

### 方法 1：查看反汇编

```bash
objdump -D -b binary -m i8086 -M intel boot.bin
```

**输出：**
```
   5:	be 15 7c             	mov    si,0x7c15
```

可以看到 `mov si, 0x7c15`，这里的 `0x7c15` 就是 `msg` 标签的地址。

### 方法 2：查看十六进制内容

```bash
hexdump -C boot.bin | head -2
```

**输出：**
```
00000000  b8 03 00 cd 10 be 15 7c  b4 0e ac 84 c0 74 04 cd  |.......|.....t..|
```

可以看到：
- 偏移 `0x00000000`：`b8 03 00` = `mov ax, 0x0003`
- 偏移 `0x00000003`：`cd 10` = `int 0x10`
- 偏移 `0x00000005`：`be 15 7c` = `mov si, 0x7c15` ← **这里体现了 org 0x7C00**

### 方法 3：查看字符串位置

```bash
strings -a -t x boot.bin
```

**输出：**
```
   15 Hello from Boot Sector!
```

可以看到字符串在文件偏移 `0x15`，对应内存地址 `0x7C00 + 0x15 = 0x7C15`。

---

## 如果没有 `org 0x7C00` 会怎样？

### 假设没有 `org` 指令

如果源代码中没有 `org 0x7C00`：

```asm
; 没有 org 指令
    mov si, msg
msg db "Hello from Boot Sector!", 0
```

**汇编器会假设起始地址是 `0x0000`：**
- `msg` 标签的地址会被计算为 `0x0015`（相对于 0x0000）
- `mov si, msg` 会编译成 `mov si, 0x0015`
- 机器码：`BE 15 00`

**问题：**
- 当 BIOS 将引导扇区加载到 `0x7C00` 时
- 字符串实际在内存地址 `0x7C15`
- 但代码会尝试访问 `0x0015`（错误的位置）
- **程序会崩溃或显示乱码**

---

## 总结

**`org 0x7C00` 在编译后代码中的体现：**

1. **直接体现**：`mov si, msg` 被编译成 `mov si, 0x7C15`
   - 机器码：`BE 15 7C`
   - 地址 `0x7C15` 是相对于 `org 0x7C00` 计算的

2. **间接体现**：所有标签地址都基于 `0x7C00` 计算
   - `start` 标签 = `0x7C00`
   - `.print` 标签 = `0x7C0A`
   - `.halt` 标签 = `0x7C13`
   - `msg` 标签 = `0x7C15`

3. **验证方法**：
   - 查看反汇编：`objdump` 显示 `mov si,0x7c15`
   - 查看十六进制：`hexdump` 显示 `be 15 7c`
   - 查看字符串位置：`strings` 显示偏移 `0x15`，对应地址 `0x7C15`

---

## 相关文档

- [BOOT_SECTOR_ANALYSIS.md](BOOT_SECTOR_ANALYSIS.md) - 引导扇区代码手工分析指南
- [boot.asm](boot.asm) - 源代码文件
