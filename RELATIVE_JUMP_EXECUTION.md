# 相对跳转指令的执行过程：CPU 如何得知偏移量

## 关键点

**CPU 在执行 `je` 指令时，偏移量直接存储在机器码中，CPU 从指令本身读取偏移量，然后根据当前 IP（指令指针）寄存器计算目标地址。**

---

## 指令格式

### 反汇编显示 vs 实际机器码

**反汇编输出（objdump）：**
```
   d:	74 04                	je     0x13
```

**实际机器码（十六进制）：**
```
74 04
```

**关键理解：**
- `0x13` 是反汇编器显示的**绝对地址**（方便阅读）
- `0x04` 是实际存储在机器码中的**相对偏移量**
- CPU 执行时使用的是相对偏移量，不是绝对地址

---

## CPU 执行过程

### 步骤 1：CPU 读取指令

当 CPU 执行到地址 `0x7C0D` 时：

```
内存地址 0x7C0D: 74 04
         │   │
         │   └─ 相对偏移量（有符号 8 位）
         └─ je/jz 指令操作码
```

**CPU 的操作：**
1. 从内存地址 `0x7C0D` 读取字节 `0x74`
2. 解码：`0x74` = `je/jz`（条件跳转，如果 ZF=1）
3. 检查零标志位（ZF）：
   - 如果 ZF=1（上一条指令 `test al, al` 结果为 0），执行跳转
   - 如果 ZF=0，不跳转，继续执行下一条指令

### 步骤 2：如果条件满足，读取偏移量

如果 ZF=1（需要跳转）：

```
CPU 从内存地址 0x7C0E 读取偏移量：0x04
```

**偏移量的含义：**
- `0x04` 是有符号 8 位整数
- 正值表示向前跳转
- 负值表示向后跳转

### 步骤 3：计算目标地址

**CPU 的计算过程：**

```
当前指令地址 = 0x7C0D（IP 寄存器的值）
指令长度 = 2 字节（74 04）
相对偏移量 = 0x04（从指令中读取）

目标地址 = 当前指令地址 + 指令长度 + 相对偏移量
        = 0x7C0D + 2 + 0x04
        = 0x7C0D + 0x06
        = 0x7C13
```

**关键点：**
- CPU 使用**当前 IP（指令指针）寄存器**的值作为基准
- IP 寄存器在实模式下是 16 位，指向当前执行的指令
- 计算是**硬件自动完成**的，不需要程序干预

### 步骤 4：更新 IP 寄存器

```
IP = 0x7C13（跳转到目标地址）
```

CPU 继续从 `0x7C13` 执行下一条指令。

---

## 详细执行流程

### 完整执行序列

```
1. CPU 执行到地址 0x7C0B: test al, al
   └─ 设置 ZF 标志位（如果 al == 0，ZF = 1）

2. CPU 执行到地址 0x7C0D: je 0x7C13
   ├─ 读取操作码：0x74（je/jz）
   ├─ 检查 ZF 标志位
   │
   ├─ [如果 ZF = 1]（al == 0，字符串结束）
   │  ├─ 读取偏移量：0x04（从地址 0x7C0E）
   │  ├─ 计算目标地址：0x7C0D + 2 + 0x04 = 0x7C13
   │  └─ 更新 IP = 0x7C13，跳转到 .halt
   │
   └─ [如果 ZF = 0]（al != 0，还有字符）
      ├─ 不读取偏移量
      ├─ 不计算目标地址
      └─ IP = 0x7C0D + 2 = 0x7C0F，继续执行下一条指令（int 0x10）
```

---

## 为什么使用相对偏移量？

### 优势

1. **代码可重定位**：
   - 相对跳转不依赖绝对地址
   - 代码可以在内存中任意位置执行
   - 适合引导扇区（可能加载到不同地址）

2. **指令长度短**：
   - 相对跳转只需要 1 字节偏移量（-128 到 +127）
   - 绝对跳转需要 2 字节地址（16 位）
   - 节省代码空间

3. **执行效率高**：
   - CPU 硬件直接计算，速度快
   - 不需要额外的内存访问

### 限制

- **跳转范围有限**：只能跳转 -128 到 +127 字节
- **如果跳转距离超过 127 字节，需要使用 `jmp near` 或 `jmp far`**

---

## 其他相对跳转指令示例

### `jmp .print`（向后跳转）

**源代码：**
```asm
jmp .print
```

**机器码：**
```
EB F7
```

**执行过程：**
```
当前地址 = 0x7C11
偏移量 = 0xF7（有符号：0xF7 = -9）
目标地址 = 0x7C11 + 2 + (-9) = 0x7C0A
```

### `jmp $`（无限循环）

**源代码：**
```asm
jmp $
```

**机器码：**
```
EB FE
```

**执行过程：**
```
当前地址 = 0x7C13
偏移量 = 0xFE（有符号：0xFE = -2）
目标地址 = 0x7C13 + 2 + (-2) = 0x7C13（跳回自己）
```

---

## 与绝对地址跳转的对比

### 相对跳转（`je`）

```
机器码：74 04
- 操作码：74（1 字节）
- 偏移量：04（1 字节，有符号）
- 总长度：2 字节
- 跳转范围：-128 到 +127 字节
```

### 绝对跳转（`jmp far`）

```
机器码：EA 13 7C 00 00
- 操作码：EA（1 字节）
- 偏移量：13 7C（2 字节，小端序）
- 段地址：00 00（2 字节）
- 总长度：5 字节
- 跳转范围：整个段（64KB）
```

---

## 关键要点总结

1. **偏移量存储在指令中**：
   - `je` 指令的机器码是 `74 04`
   - `04` 就是相对偏移量，直接存储在内存中

2. **CPU 自动读取和计算**：
   - CPU 从指令中读取偏移量（`0x04`）
   - CPU 使用当前 IP 寄存器值（`0x7C0D`）作为基准
   - CPU 硬件自动计算：`目标地址 = IP + 指令长度 + 偏移量`

3. **不需要 `org` 指令参与执行**：
   - `org 0x7C00` 只在**编译时**影响偏移量的计算
   - **执行时**，CPU 只关心相对偏移量和当前 IP 值
   - 只要代码加载到正确位置（0x7C00），相对跳转就能正确工作

4. **为什么需要 `org 0x7C00`**：
   - 汇编器需要知道绝对地址，才能计算正确的相对偏移量
   - 如果 `org` 错误，汇编器会计算出错误的偏移量
   - 但一旦编译完成，偏移量就固定了，CPU 执行时只使用这个固定的偏移量

---

## 相关文档

- [ORG_0x7C00_EXPLANATION.md](ORG_0x7C00_EXPLANATION.md) - org 0x7C00 在编译后代码中的体现
- [BOOT_SECTOR_ANALYSIS.md](BOOT_SECTOR_ANALYSIS.md) - 引导扇区代码手工分析指南
