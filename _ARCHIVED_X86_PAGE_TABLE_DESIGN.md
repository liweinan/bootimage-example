# x86-64 多级页表设计详解

## 文档定位

本文档深入讲解 x86-64 架构中的**多级页表设计原理**，从页表的建立过程、设计理由到动态管理机制，提供全面的技术细节。

**前置阅读**（建议先阅读，理解设计动机）：
- [为什么需要虚拟内存](WHY_VIRTUAL_MEMORY.md) - 理解为什么要设计页表
  - 为什么不直接用物理地址？
  - 为什么需要多级页表而不是一级页表？
  - 数学证明：多级页表如何节省内存（512GB vs 68KB）

**核心内容**:
- 页表的建立过程和时间线
- 内核启动早期（阶段2-3）的分页目的和依据
- 多级页表 vs 一级页表的设计权衡
- 书籍目录类比：直观理解多级页表
- 页表的动态管理机制

**适合读者**:
- 想深入理解 x86-64 内存管理的开发者
- 需要了解页表实现细节的系统程序员
- 对操作系统底层机制感兴趣的学习者

**相关文档**:
- [GDT 详解：从保护模式到长模式](X86_MEMORY_MANAGEMENT_THEORY.md) - GDT 与页表的协作关系
- [Linux 内核分页机制完整指南](LINUX_MEMORY_MANAGEMENT_EVOLUTION.md) - 分页机制的完整演化

---

## 一、页表的建立过程

### ❓ 关键问题：页表是谁建立的？什么时候建立的？

页表不是硬件自动生成的，而是**内核代码**在启动过程中创建和填充的。

### 📋 页表建立的时间线

| 阶段 | 页表名称 | 建立者 | 映射方式 | 代码位置 |
|------|---------|-------|---------|---------|
| **GRUB** | GRUB自己的页表 | GRUB Bootloader | Identity mapping | GRUB代码 |
| **Compressed Kernel** | `level4_pgt` | arch/x86/boot/compressed/head_64.S | Identity mapping (1:1) | `startup_32` |
| **Main Kernel Early** | `early_level4_pgt` | arch/x86/kernel/head_64.S | Identity + Direct mapping | `__startup_64` |
| **Runtime** | 动态页表 | mm/init.c, mm/pgtable.c | 多种映射方式 | `paging_init()` |

### 🔨 页表的建立过程（以 Compressed Kernel 为例）

**代码**：`arch/x86/boot/compressed/head_64.S`

```asm
# 1. 定义页表存储区域（在 .bss 段）
.section ".pgtable","aw",@nobits
.balign 4096
level4_pgt:
    .fill 512, 8, 0          # PML4: 512个条目，每个8字节，初始化为0
level3_pgt:
    .fill 512, 8, 0          # PDPT: 512个条目
level2_pgt:
    .fill 4*512, 8, 0        # PD: 4个表，每个512条目（映射4GB）

# 2. 填充页表（在启动代码中）
startup_32:
    # 填充 PML4 表
    leal    level3_pgt(%ebp), %eax
    orl     $0x3, %eax                # P=1, R/W=1
    movl    %eax, level4_pgt(%ebp)    # PML4[0] → level3_pgt

    # 填充 PDPT 表
    leal    level2_pgt(%ebp), %eax
    orl     $0x3, %eax
    movl    %eax, level3_pgt(%ebp)    # PDPT[0] → level2_pgt

    # 填充 PD 表（使用 2MB 大页）
    movl    $0x183, %eax              # 物理地址 0, P=1, R/W=1, PS=1
    movl    $512, %ecx                # 循环次数：512个2MB页 = 1GB
1:
    movl    %eax, level2_pgt(%ebp, %edi, 8)  # 写入 PD 表
    addl    $0x200000, %eax           # 下一个 2MB 页框
    incl    %edi
    loop    1b                        # 继续循环
```

### 💡 详细解释

**步骤1：分配存储空间**
```
内存布局（假设 level4_pgt 在 0x10000）：
0x10000: level4_pgt (4KB, 512 × 8 字节)
0x11000: level3_pgt (4KB, 512 × 8 字节)
0x12000: level2_pgt (16KB, 4 × 512 × 8 字节)
```

**步骤2：建立链接关系**
```
PML4[0] = level3_pgt 的物理地址 | 0x3
        = 0x11003 (P=1, R/W=1)
        → "线性地址 0x0000_0000_0000_0000 - 0x0000_007F_FFFF_FFFF"
          使用 PDPT 表 level3_pgt

PDPT[0] = level2_pgt 的物理地址 | 0x3
        = 0x12003
        → "线性地址 0x0000_0000_0000_0000 - 0x0000_0000_3FFF_FFFF"
          使用 PD 表 level2_pgt
```

**步骤3：填充最终映射（2MB 大页）**
```
第 0 个循环：
  PD[0] = 0x0000000000000183
        = 物理页框 0x00000000 | P=1 | R/W=1 | PS=1
        → 线性地址 0x000000 - 0x1FFFFF 映射到物理地址 0x000000 - 0x1FFFFF

第 1 个循环：
  PD[1] = 0x0000000000200183
        = 物理页框 0x00200000 | P=1 | R/W=1 | PS=1
        → 线性地址 0x200000 - 0x3FFFFF 映射到物理地址 0x200000 - 0x3FFFFF

第 2 个循环：
  PD[2] = 0x0000000000400183
        = 物理页框 0x00400000 | ...

... （继续填充 512 个条目，覆盖 1GB）
```

### 🎯 关键理解

1. **页表项的值是代码写入的**：
   - `PD[0] = 0x183` 是代码显式写入的
   - `PD[90] = 0x0B400183` 也是代码计算后写入的
   - 不是硬件自动生成的

2. **映射关系是内核决定的**：
   - Identity mapping: `PD[i] = i × 0x200000 | 0x183`
   - Direct mapping: `PD[i] = (i × 0x200000 - PAGE_OFFSET) | 0x183`
   - 用户空间: 动态分配，`PD[i]` 可以指向任意物理页框

3. **不同线性地址通过索引找到对应页表项**：
   ```
   线性地址 0x0000000000000000 → PML4[0] → PDPT[0] → PD[0]   → 物理 0x000000
   线性地址 0x0000000000200000 → PML4[0] → PDPT[0] → PD[1]   → 物理 0x200000
   线性地址 0x00007F85A0000000 → PML4[254]→ PDPT[508]→ PD[90] → 物理 0xB400000
   ```

4. **每个 offset 地址不一定都有映射**：
   - 如果 `P=0`（Present位），访问会触发 **Page Fault**
   - 内核可以懒分配：只在实际使用时才建立映射
   - 用户空间的 `mmap()` / `malloc()` 都是懒分配

### 📚 参考代码位置

- Compressed Kernel 页表：`arch/x86/boot/compressed/head_64.S` (startup_32)
- Main Kernel Early 页表：`arch/x86/kernel/head_64.S` (__startup_64)
- Runtime 页表初始化：`arch/x86/mm/init.c` (paging_init)
- 页表操作函数：`arch/x86/mm/pgtable.c`

### 🧮 实战计算：映射 1MB 区域需要多少页表项？

让我们以一个具体例子说明：为线性地址区域 `[0x00100000, 0x00200000)` 建立 Identity Mapping（1MB 区域）。

#### 方案 A：使用 4KB 小页（精确映射）

```
内存区域大小：0x00200000 - 0x00100000 = 1MB = 1,048,576 字节
每个页大小：4KB = 4,096 字节
所需页数：1,048,576 ÷ 4,096 = 256 页
```

**页表项（PTE）数量**：
- **需要 256 个页表项（PTE）**
- 这些 PTE 位于一个**页表（Page Table）**中
- 每个页表项大小 8 字节，一个页表（4KB）可容纳 512 个 PTE
- 所以 256 个 PTE 只占用一个页表的一半

**页目录项（PDE）数量**：
- 区域 `[0x00100000, 0x00200000)` 完全位于第 0 个页目录项管理范围
- 每个 PDE 管理 `512 × 4KB = 2MB` 的地址空间
- **只需要 1 个页目录项（PDE）**

**总设置**：1 个 PDE + 1 个页表（含 256 个有效 PTE）

**示例映射关系**：
```
线性地址 0x00100000 → PD[0] → PT[256] → 物理地址 0x00100000
线性地址 0x00101000 → PD[0] → PT[257] → 物理地址 0x00101000
线性地址 0x00102000 → PD[0] → PT[258] → 物理地址 0x00102000
...
线性地址 0x001FF000 → PD[0] → PT[511] → 物理地址 0x001FF000
```

**内存开销**：
```
页表项：256 × 8 字节 = 2,048 字节（约 2KB）
页目录项：1 × 8 字节 = 8 字节
总开销：约 2KB（精确控制）
```

#### 方案 B：使用 4MB 大页（简单但过度映射）

```
内存区域大小：1MB
每个大页大小：4MB
```

**问题**：1MB < 4MB，使用大页会**额外映射 3MB 不需要的内存**

**如果映射对齐的 4MB 区域** `[0x00000000, 0x00400000)`：
- **只需 1 个页目录项（PDE）**
- PDE 的 PS 位 = 1，直接指向 4MB 物理页框
- **没有页表（Page Table）**这一级

**示例**：
```
PDE[0]: 基址 = 0x00000000, PS = 1 (大页标志)
结果：线性地址 0x00000000 - 0x003FFFFF → 物理地址 0x00000000 - 0x003FFFFF
      覆盖你需要的 [0x00100000, 0x00200000) 区域
```

**内存开销**：
```
页目录项：1 × 8 字节 = 8 字节
但过度映射了：4MB - 1MB = 3MB 额外区域
```

#### 方案对比总结

| 方案 | 页大小 | 页表项数量 | 内存开销 | 优点 | 缺点 | 适用场景 |
|------|--------|------------|----------|------|------|----------|
| **精确映射** | 4KB | 256 个 PTE<br>+ 1 个 PDE | ~2KB | 无内存浪费<br>精确控制 | 设置复杂<br>容易遗漏映射 | 内存紧张<br>需要精确控制 |
| **大页映射** | 4MB | 1 个 PDE | 8 字节 | **简单可靠**<br>安全性高 | 过度映射 3MB | **启动阶段**<br>（最常见）|

**为什么启动代码通常选择大页？**

在 `mov cr0, eax` 启用分页的瞬间，下一条指令的线性地址必须能成功翻译。使用 4KB 页：
- 必须确保**当前指令指针所在页**和**下一条指令可能所在的页**都被映射
- 必须确保**栈访问**（push/pop）所在的页也被映射
- 必须确保**访问页表本身**的地址也被映射

**大页的好处**：一个 4MB 大页覆盖了足够大的连续空间，几乎可以保证初期执行流、栈和关键数据结构都在安全范围内。

**实际内核代码示例**（Linux 启动）：
```asm
# arch/x86/boot/compressed/head_64.S
# 使用 2MB 大页映射低端 1GB 物理内存
movl    $512, %ecx                # 循环次数：512 个 2MB 页 = 1GB
movl    $0x183, %eax              # 物理地址 0, P=1, R/W=1, PS=1
1:
    movl    %eax, level2_pgt(%ebp, %edi, 8)  # 写入 PD 表
    addl    $0x200000, %eax       # 下一个 2MB 页框
    incl    %edi
    loop    1b
```

这个循环创建了 512 个 PDE，每个映射 2MB，总共覆盖 1GB，使用大页简化了启动阶段的页表设置。

---

## 二、阶段2和3的分页目的和依据

### ❓ 核心问题：还没有用户进程时，为什么需要分页？分页的依据是什么？

这是一个容易混淆的关键点。让我们澄清几个重要概念：

### 🔧 问题1：分页的目的是什么？

**常见误解**：分页只是为了用户进程的地址空间隔离

**真相**：分页在内核启动早期有**两个完全不同的目的**

| 阶段 | 分页目的 | 是否有用户进程 | 映射类型 |
|------|---------|--------------|---------|
| **阶段2-3<br>（内核启动）** | ✅ **硬件要求**：<br>x86-64 Long Mode 强制要求启用分页<br>✅ **内核运行**：<br>让内核代码能在虚拟地址运行 | ❌ 没有 | Identity Mapping<br>（线性地址 = 物理地址） |
| **阶段4<br>（运行时）** | ✅ **地址隔离**：<br>每个进程独立的虚拟地址空间<br>✅ **内存管理**：<br>懒分配、写时复制、交换等 | ✅ 有 | Direct Mapping<br>（内核空间）<br>+ 进程独立映射<br>（用户空间） |

### 🚨 x86-64 硬件强制要求

**关键事实**：x86-64 架构**无法在分页未启用时进入 64 位模式**

```
x86-64 Long Mode 激活条件（缺一不可）：
┌─────────────────────────────────────────────────────┐
│ 1. CR4.PAE = 1     ← Physical Address Extension     │
│ 2. CR3 = 页表基址   ← 必须有有效的页表               │
│ 3. EFER.LME = 1    ← Long Mode Enable               │
│ 4. CR0.PG = 1      ← Paging Enable ← 必须启用分页！  │
└─────────────────────────────────────────────────────┘

如果 CR0.PG = 0（分页未启用）：
• CPU 拒绝进入 Long Mode
• 无法执行 64 位代码
• 即使没有用户进程也必须建立页表
```

**代码证据**：`arch/x86/boot/compressed/head_64.S`

```asm
# 启用 Long Mode 的关键步骤
startup_32:
    # 1. 启用 PAE
    movl    %cr4, %eax
    orl     $X86_CR4_PAE, %eax
    movl    %eax, %cr4

    # 2. 加载页表基址到 CR3
    leal    level4_pgt(%ebp), %eax
    movl    %eax, %cr3        # ← 必须先设置页表

    # 3. 启用 Long Mode
    movl    $MSR_EFER, %ecx
    rdmsr
    btsl    $_EFER_LME, %eax
    wrmsr

    # 4. 启用分页
    movl    %cr0, %eax
    orl     $X86_CR0_PG, %eax # ← 启用分页
    movl    %eax, %cr0        # ← 此时才能进入 Long Mode
```

### 🎯 问题2：阶段2和3的页表映射的是什么？

**答案**：映射**内核自身的代码和数据**，使用 **Identity Mapping（恒等映射）**

#### 什么是 Identity Mapping？

```
Identity Mapping（恒等映射）：
┌─────────────────────────────────────────────────┐
│ 线性地址 == 物理地址                             │
├─────────────────────────────────────────────────┤
│ 线性 0x00000000 → 物理 0x00000000               │
│ 线性 0x00100000 → 物理 0x00100000               │
│ 线性 0x01000000 → 物理 0x01000000               │
│ 线性 0x10000000 → 物理 0x10000000               │
│      └────┬────┘        └────┬────┘             │
│          相同                相同                 │
└─────────────────────────────────────────────────┘

页表内容（2MB 大页）：
PD[0]   = 0x0000000000000183  → 物理页框 0x00000000
PD[1]   = 0x0000000000200183  → 物理页框 0x00200000
PD[2]   = 0x0000000000400183  → 物理页框 0x00400000
...
PD[511] = 0x000000003FE00183  → 物理页框 0x3FE00000

规律：PD[i] = (i × 0x200000) | 0x183
```

#### 为什么使用 Identity Mapping？

**目的**：让内核代码可以"透明"运行，地址不变

```
场景：内核代码位于物理地址 0x01000000

没有分页（32位模式）：
  代码中的地址：mov %rax, 0x01000000
  → 访问物理地址 0x01000000 ✓

启用分页（64位模式）+ Identity Mapping：
  代码中的地址：mov %rax, 0x01000000
  → 段式转换：线性地址 = 0 + 0x01000000 = 0x01000000
  → 分页转换：物理地址 = 0x01000000 (恒等映射)
  → 访问物理地址 0x01000000 ✓

关键：代码不需要修改，地址计算结果相同！
```

### 📊 问题3：CR3 控制的是什么？

**常见误解**：CR3 控制某个进程使用的页表

**准确理解**：CR3 控制**当前活动的页表基址**，不限于进程页表

| 阶段 | CR3 指向 | 页表用途 | 谁在使用 |
|------|---------|---------|---------|
| **阶段2** | `level4_pgt` 物理地址 | 内核引导页表<br>（Identity Mapping） | Compressed kernel 代码 |
| **阶段3** | `early_level4_pgt` 物理地址 | 内核早期页表<br>（Identity + Direct Mapping） | Main kernel early 代码 |
| **阶段4<br>内核态** | `init_mm.pgd` 物理地址 | 内核页表<br>（Direct Mapping） | 内核线程<br>（kthreadd, kworker, etc） |
| **阶段4<br>用户态** | `current->mm->pgd` 物理地址 | 用户进程页表<br>（用户空间独立 + 内核空间共享） | 用户进程<br>（bash, ls, etc） |

### 🔍 完整示例：阶段2的地址转换

**场景**：Compressed kernel 执行 `mov %rax, 0x01234567`

```
┌────────────────────────────────────────────────────────┐
│ 【阶段2：Compressed Kernel，64位模式，分页已启用】      │
├────────────────────────────────────────────────────────┤
│ 1. 逻辑地址：__KERNEL_DS:0x01234567                    │
│                                                         │
│ 2. 段式转换（GDT）：                                    │
│    GDT[3] (__KERNEL_DS) → Base = 0x00000000            │
│    线性地址 = 0 + 0x01234567 = 0x01234567              │
│                                                         │
│ 3. 分页转换（CR3 → level4_pgt）：                      │
│    CR3 = level4_pgt 的物理地址（假设 0x10000）         │
│                                                         │
│    提取索引：                                           │
│    线性地址 0x01234567 = 0000_0000_0001_0010_0011_0100_0101_0110_0111│
│    PML4 索引 = bits [47:39] = 0                        │
│    PDPT 索引 = bits [38:30] = 0                        │
│    PD 索引   = bits [29:21] = 9                        │
│    偏移      = bits [20:0]  = 0x034567                 │
│                                                         │
│    查询页表：                                           │
│    PML4[0] → PDPT 基址                                 │
│    PDPT[0] → PD 基址                                   │
│    PD[9]   = 9 × 0x200000 | 0x183 = 0x01200183        │
│             → 物理页框 0x01200000                      │
│                                                         │
│ 4. 计算物理地址：                                       │
│    物理地址 = 0x01200000 + 0x034567 = 0x01234567       │
│                                      └────┬────┘       │
│                                        与线性地址相同！  │
│                                                         │
│ ✓ Identity Mapping：线性地址 = 物理地址                │
└────────────────────────────────────────────────────────┘
```

### 🎯 关键理解

1. **分页不只是为了进程隔离**：
   - x86-64 硬件要求：Long Mode 必须启用分页
   - 即使没有用户进程，也必须建立页表

2. **阶段2和3使用 Identity Mapping**：
   - 线性地址 = 物理地址
   - 目的：让内核代码透明运行
   - CR3 指向内核引导页表，不是进程页表

3. **"分页的依据"问题的答案**：
   - 不是依据"某个用户进程的虚拟地址"
   - 依据是"x86-64 架构要求 + 内核代码的物理位置"
   - 页表建立 Identity Mapping，使线性地址 = 物理地址

4. **从 Identity Mapping 到 Direct Mapping 的过渡**：
   - 阶段2：完全 Identity Mapping
   - 阶段3：混合（部分 Identity + 部分 Direct）
   - 阶段4：主要 Direct Mapping（内核在高地址 0xFFFF...）

### 📚 深入阅读

- 完整的分页机制演化：[Linux 内核分页机制完整指南](LINUX_MEMORY_MANAGEMENT_EVOLUTION.md)
- Identity vs Direct Mapping 详解：LINUX_MEMORY_MANAGEMENT_EVOLUTION.md 第2-3部分
- x86-64 Long Mode 激活过程：LINUX_KERNEL_INIT.md 阶段2-3

---

## 三、多级页表设计原理：为什么不是一级？

### ❓ 常见问题

1. **多级页表是把大页拆分成小页吗？**
2. **Slab 是在小页基础上更细的颗粒度吗？**
3. **多级页表比一级页表更快吗？**

让我们逐一澄清这些概念。

### 🔍 问题1：多级页表 vs 页大小（两个独立概念）

**常见误解**：四级/五级页表是把大页拆分成小页

**真相**：**多级页表**和**页大小**是两个完全独立的概念！

```
┌────────────────────────────────────────────────────────┐
│ 概念1：多级页表（页表的组织结构）                       │
├────────────────────────────────────────────────────────┤
│ • 含义：页表的层级关系                                  │
│ • 结构：PML4 → PDPT → PD → PT                         │
│ • 决定：如何查找页表项                                  │
│ • 例子：4 级页表、5 级页表                             │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│ 概念2：页大小（物理页的粒度）                           │
├────────────────────────────────────────────────────────┤
│ • 含义：物理页的大小                                    │
│ • 大小：4KB、2MB、1GB                                  │
│ • 决定：每个页表项映射多大的物理内存                     │
│ • 控制：页表项的 PS 位（Page Size）                    │
└────────────────────────────────────────────────────────┘
```

**关键**：多级页表可以映射**任何大小**的页！

**示例**：

```
场景1：4 级页表 + 2MB 大页
┌──────┐    ┌──────┐    ┌──────┐    ┌──────────┐
│ PML4 │───→│ PDPT │───→│  PD  │───→│ 2MB 物理页│
└──────┘    └──────┘    └──────┘    └──────────┘
                          PS=1        (停在 PD 层，不需要 PT)

场景2：4 级页表 + 4KB 小页
┌──────┐    ┌──────┐    ┌──────┐    ┌────┐    ┌──────────┐
│ PML4 │───→│ PDPT │───→│  PD  │───→│ PT │───→│ 4KB 物理页│
└──────┘    └──────┘    └──────┘    └────┘    └──────────┘
                                      PS=0      (需要 PT 层)

场景3：5 级页表 + 1GB 巨页
┌──────┐    ┌──────┐    ┌──────┐    ┌──────────┐
│ PML5 │───→│ PML4 │───→│ PDPT │───→│ 1GB 物理页│
└──────┘    └──────┘    └──────┘    └──────────┘
                          PS=1        (停在 PDPT 层)

场景4：4 级页表同时使用混合页大小
虚拟地址 0x00400000 → PML4 → PDPT → PD → PT → 4KB 页（代码段）
虚拟地址 0x60000000 → PML4 → PDPT → PD → 2MB 页（数据库缓存）
```

**结论**：
- **多级页表**：查找结构（如何找到页表项）
- **页大小**：映射粒度（每次映射多大内存）
- 两者独立，可任意组合

### 🔍 问题2：Slab vs Buddy System

**您的理解完全正确！** Slab 确实是在物理页基础上更细的颗粒度。

```
Linux 内存分配器层次：
┌───────────────────────────────────────────────────────┐
│ Layer 1: Buddy System（伙伴系统）                     │
├───────────────────────────────────────────────────────┤
│ • 分配单位：2^n 个连续物理页                           │
│   - 1 页（4KB）                                       │
│   - 2 页（8KB）                                       │
│   - 4 页（16KB）                                      │
│   - 8 页（32KB）                                      │
│   - ...                                               │
│   - 1024 页（4MB）                                    │
│                                                       │
│ • 最小粒度：4KB（一个物理页）                          │
│ • 用途：大块内存分配（多页分配）                        │
│ • 特点：2 的幂次方分配，可能浪费                        │
└───────────────────────────────────────────────────────┘
                         ↓ 从 Buddy 申请 4KB 页
┌───────────────────────────────────────────────────────┐
│ Layer 2: Slab/SLUB/SLOB 分配器                       │
├───────────────────────────────────────────────────────┤
│ • 从 Buddy System 申请 4KB 页                        │
│ • 切分成更小的固定大小对象：                           │
│   - 32 字节（小对象）                                 │
│   - 64 字节                                           │
│   - 96 字节                                           │
│   - 128 字节                                          │
│   - 256 字节                                          │
│   - 512 字节                                          │
│   - ...                                               │
│   - 4096 字节（整页）                                 │
│                                                       │
│ • 最小粒度：可以小到几十字节                           │
│ • 用途：内核对象分配（频繁分配/释放的小对象）           │
│ • 特点：减少碎片、提高缓存局部性                        │
└───────────────────────────────────────────────────────┘

常见的 Slab 缓存：
• kmalloc-32:     task_struct 的一部分
• kmalloc-64:     小型内核对象
• kmalloc-192:    inode 结构
• kmalloc-4096:   整页对象
• dentry:         目录项缓存
• inode_cache:    inode 缓存
• task_struct:    进程描述符
```

**具体例子**：分配一个 `task_struct`（假设大小 9216 字节）

```
步骤1：Slab 初始化
• Slab 创建 task_struct 缓存
• 从 Buddy System 申请 3 个 4KB 页（12KB）
• 划分：9216B（1个 task_struct）+ 2880B（剩余）

步骤2：进程创建（fork）
• 调用 kmem_cache_alloc(task_struct_cache)
• 直接从 Slab 缓存获取（无需访问 Buddy System）
• 时间：O(1)

步骤3：进程退出（exit）
• 调用 kmem_cache_free(task_struct_cache, task)
• 归还到 Slab 缓存（不归还给 Buddy System）
• 下次 fork 可立即重用

优势：
• 减少 Buddy System 调用（慢）
• 减少内存碎片
• 对象预初始化（构造/析构）
• 缓存热数据（提高性能）
```

**颗粒度对比**：

| 分配器 | 最小单位 | 用途 | 示例 |
|--------|---------|------|------|
| **Buddy System** | 4KB（1页） | 大块内存 | 分配多个页给进程 |
| **Slab** | 几十字节 | 小对象 | task_struct（9KB）<br>dentry（192B） |

**结论**：Slab 建立在 Buddy System 之上，提供更细的颗粒度 ✓

### 🔍 问题3：多级页表速度对比

**核心答案：多级页表实际上更慢，但节省巨大内存！**

#### 内存开销对比

**一级页表（线性页表）**：

```
假设条件：
• 64 位地址空间（实际使用 48 位）
• 4KB 页大小
• 每个页表项 8 字节

虚拟地址空间：2^48 字节 = 256 TB
需要的页数：2^48 / 2^12 = 2^36 = 68,719,476,736 页
页表大小：2^36 × 8 字节 = 549,755,813,888 字节 ≈ 512 GB

┌────────────────────────────────────────┐
│ 单一巨大的页表（512 GB）                │
├────────────────────────────────────────┤
│ [0] → 物理页框 0                       │
│ [1] → 物理页框 1                       │
│ [2] → 物理页框 2                       │
│ ...                                    │
│ [68,719,476,736] → 物理页框 N          │
└────────────────────────────────────────┘

每个进程需要 512 GB 页表内存！
```

**多级页表（四级页表）**：

```
实际进程的虚拟地址使用情况：
┌────────────────────────────────────────┐
│ 代码段（Text）：     4 MB              │
│ 数据段（Data+BSS）： 4 MB              │
│ 堆（Heap）：         8 MB              │
│ 栈（Stack）：        4 MB              │
│ 共享库（libc等）：   16 MB             │
│ ─────────────────────────────────────  │
│ 总计：约 36 MB 虚拟地址空间            │
└────────────────────────────────────────┘

需要的页表：
• 1 个 PML4 表（4KB）           ← 总是需要 1 个
• 2 个 PDPT 表（8KB）           ← 低地址 1 个 + 高地址 1 个
• 5 个 PD 表（20KB）            ← 覆盖 36MB
• 9 个 PT 表（36KB）            ← 每个 PT 覆盖 2MB
────────────────────────────────
总计：约 68 KB

节省比例：512 GB / 68 KB ≈ 7,529,536 倍！
```

**极端对比**：进程只使用 4 个虚拟页（16KB）

```
虚拟地址使用：
• 0x00400000（代码段第 1 页）
• 0x00600000（代码段第 N 页）
• 0x7FFFFFE000（栈顶）
• 0x7F8000000000（共享库）

一级页表：仍需 512 GB（必须覆盖整个地址空间）

四级页表：
• 1 个 PML4（4KB）
• 2 个 PDPT（8KB）
• 4 个 PD（16KB）
• 4 个 PT（16KB）
────────────────
总计：44 KB

结论：即使只用 16KB 虚拟内存，一级页表仍需 512GB！
```

#### 速度对比

**理论速度（无 TLB 缓存）**：

```
一级页表查找：
1. 虚拟地址直接索引页表
2. 读取页表项（1 次内存访问）
3. 得到物理页框号
────────────────
总计：1 次内存访问（~100ns）

四级页表查找：
1. CR3 → 读取 PML4 表项（1 次内存访问）
2. PML4 → 读取 PDPT 表项（1 次内存访问）
3. PDPT → 读取 PD 表项（1 次内存访问）
4. PD → 读取 PT 表项（1 次内存访问）
5. 得到物理页框号
────────────────
总计：4 次内存访问（~400ns）

五级页表查找：
总计：5 次内存访问（~500ns）
```

| 页表类型 | 内存访问次数 | 延迟（100ns/次） |
|---------|------------|-----------------|
| 一级页表 | 1 次 | 100 ns |
| 四级页表 | 4 次 | 400 ns |
| 五级页表 | 5 次 | 500 ns |

**理论上，一级页表快 4 倍！**

#### MMU 硬件页表遍历伪代码

为了更直观地理解多级页表查找的过程，这里给出 MMU（内存管理单元）硬件执行的伪代码：

```c
// CPU MMU 硬件自动执行的虚拟地址翻译过程
// 注意：这是由硬件完成的，不是软件代码！
物理地址 walk_virtual_address(虚拟地址 va, cr3) {
    // ===== 第 4 级：PML4（Page Map Level 4）=====
    pml4_index = (va >> 39) & 0x1FF;           // 提取 bits [47:39]
    pml4e_addr = cr3 + pml4_index * 8;         // 计算 PML4 表项地址
    pml4e = *物理读(pml4e_addr);               // 【内存访问 #1】
    if (!pml4e.present) 触发页错误(#PF);       // P=0：页不在内存
    if (权限检查失败(pml4e)) 触发页错误(#PF);  // 检查 U/S, R/W, NX 位

    // ===== 第 3 级：PDPT（Page Directory Pointer Table）=====
    pdp_index = (va >> 30) & 0x1FF;            // 提取 bits [38:30]
    pdpe_addr = (pml4e.phys_addr << 12) + pdp_index * 8;
    pdpe = *物理读(pdpe_addr);                 // 【内存访问 #2】
    if (!pdpe.present) 触发页错误(#PF);
    if (pdpe.page_size == 1) {                 // PS=1：1GB 大页
        // 停在 PDPT 层，使用 1GB 大页
        物理地址 = (pdpe.phys_addr << 30) | (va & 0x3FFFFFFF);
        return 物理地址;                        // 完成！3 次内存访问
    }

    // ===== 第 2 级：PD（Page Directory）=====
    pd_index = (va >> 21) & 0x1FF;             // 提取 bits [29:21]
    pde_addr = (pdpe.phys_addr << 12) + pd_index * 8;
    pde = *物理读(pde_addr);                   // 【内存访问 #3】
    if (!pde.present) 触发页错误(#PF);
    if (pde.page_size == 1) {                  // PS=1：2MB 大页
        // 停在 PD 层，使用 2MB 大页
        物理地址 = (pde.phys_addr << 21) | (va & 0x1FFFFF);
        return 物理地址;                        // 完成！4 次内存访问
    }

    // ===== 第 1 级：PT（Page Table）=====
    pt_index = (va >> 12) & 0x1FF;             // 提取 bits [20:12]
    pte_addr = (pde.phys_addr << 12) + pt_index * 8;
    pte = *物理读(pte_addr);                   // 【内存访问 #4】
    if (!pte.present) 触发页错误(#PF);

    // ===== 最终物理地址计算 =====
    物理地址 = (pte.phys_addr << 12) | (va & 0xFFF);  // 4KB 页
    return 物理地址;                            // 完成！5 次内存访问
}
```

**关键点**：
1. **这是硬件自动执行的**：程序员和内核代码不需要手动调用这个过程
2. **每级都可能停止**：遇到大页（PS=1）时提前结束，不继续往下走
3. **每一步都要内存访问**：这就是为什么没有 TLB 时性能很差
4. **每一步都要权限检查**：任何一级权限检查失败都会触发 #PF
5. **TLB 缓存这个结果**：下次访问同一虚拟页时，直接从 TLB 返回物理地址

**实例：翻译虚拟地址 0xFFFF8000_00001000**

```c
虚拟地址：0xFFFF8000_00001000
二进制：  1111111111111111 1000000000000000 0000000000000001 0000000000000000

分解索引：
PML4 索引 = bits[47:39] = 511 (0x1FF)  // 内核高地址空间
PDPT 索引 = bits[38:30] = 0    (0x0)
PD 索引   = bits[29:21] = 0    (0x0)
PT 索引   = bits[20:12] = 1    (0x1)
偏移      = bits[11:0]  = 0    (0x0)

硬件执行：
1. 读 PML4[511] → 获得 PDPT 物理地址（假设 0x2000）
2. 读 PDPT[0]   → 获得 PD 物理地址（假设 0x3000）
3. 读 PD[0]     → 获得 PT 物理地址（假设 0x4000）
4. 读 PT[1]     → 获得物理页框号（假设 0x12345）
5. 物理地址 = 0x12345000 + 0x000 = 0x12345000
```

**五级页表（LA57 模式）的差别**：

```c
// 五级页表在最前面多一级 PML5
物理地址 walk_virtual_address_5level(虚拟地址 va, cr3) {
    // ===== 第 5 级：PML5 =====
    pml5_index = (va >> 48) & 0x1FF;           // 提取 bits [56:48]
    pml5e_addr = cr3 + pml5_index * 8;
    pml5e = *物理读(pml5e_addr);               // 【内存访问 #1】
    if (!pml5e.present) 触发页错误(#PF);

    // 剩余步骤与四级相同，PML4 变成第 2 级
    // 总共最多 6 次内存访问（5 级页表 + 1 次数据）
    ...
}
```

**为什么 TLB 如此重要？**

```
没有 TLB：
• 每次内存访问都要查 4-5 次页表
• 1 条指令可能有 2-3 次内存访问
• 总内存访问次数：1 条指令 × (2-3 次数据访问) × (4-5 次页表查询) = 10-20 次内存访问
• CPU 会被内存延迟拖垮！

有 TLB（95% 命中率）：
• 95% 的访问：0 次页表查询（从 TLB 直接获取）
• 5% 的访问：4-5 次页表查询
• 平均：0.05 × 4 = 0.2 次额外内存访问
• 性能可接受 ✓
```

**实际速度（有 TLB 缓存）**：

```
TLB（Translation Lookaside Buffer）：
┌────────────────────────────────────────┐
│ CPU 内置的页表查询结果缓存              │
├────────────────────────────────────────┤
│ • 容量：通常几百到几千个条目            │
│ • 内容：虚拟页号 → 物理页框号           │
│ • 命中率：典型工作负载 90-99%           │
│ • 访问延迟：< 1 ns（CPU 缓存速度）      │
└────────────────────────────────────────┘

TLB 命中时（90-99% 的情况）：
• 一级页表：0 次内存访问
• 四级页表：0 次内存访问
• 延迟：< 1 ns（从 TLB 读取）

TLB 未命中时（1-10% 的情况）：
• 一级页表：1 次内存访问（100 ns）
• 四级页表：4 次内存访问（400 ns）

平均延迟（假设 95% TLB 命中率）：
• 一级：0.95 × 1ns + 0.05 × 100ns = 5.95 ns
• 四级：0.95 × 1ns + 0.05 × 400ns = 20.95 ns

实际性能差距：约 3.5 倍（而不是 4 倍）
```

**关键**：由于 TLB 命中率很高，实际性能差距比理论小得多。

### 🎯 为什么设计多级页表？

#### 1. **节省内存**（最主要原因）

```
系统有 100 个进程：
一级页表：100 × 512 GB = 51,200 GB（页表内存）
四级页表：100 × 1 MB = 100 MB（页表内存）

差距：512 倍！

即使考虑性能损失，也必须用多级页表，
因为一级页表根本不可行（内存消耗太大）。
```

#### 2. **支持稀疏地址空间**

```
64 位虚拟地址空间布局：
┌──────────────────────────────────────┐
│ 0x0000000000000000 - 0x00007FFFFFFF │ 用户空间（低 2GB）
│   大部分未使用                        │
├──────────────────────────────────────┤
│ 0x0000008000000000 - 0x00007FFFFFFF │ 用户空间（中间）
│   完全未使用（几乎 128TB）            │
├──────────────────────────────────────┤
│ 0x00007F0000000000 - 0x00007FFFFFFF │ 共享库区域
├──────────────────────────────────────┤
│ 0xFFFF800000000000 - 0xFFFFFFFFFFFF │ 内核空间
│   部分使用                            │
└──────────────────────────────────────┘

一级页表：必须为整个 256TB 分配页表（浪费！）
多级页表：只为实际使用的区域分配页表 ✓
```

#### 3. **动态按需分配**

```
进程生命周期中的页表变化：
┌─────────────────────────────────────┐
│ 进程创建（fork）                     │
├─────────────────────────────────────┤
│ • 只分配 1 个 PML4 表（4KB）         │
│ • 其他页表尚未分配                   │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│ 进程调用 malloc()                   │
├─────────────────────────────────────┤
│ • 内核分配新的 PT 表                 │
│ • 更新 PD 表项                       │
│ • 懒分配：只在访问时才建立物理映射   │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│ 进程调用 munmap()                   │
├─────────────────────────────────────┤
│ • 释放对应的 PT 表                   │
│ • 回收页表内存                       │
└─────────────────────────────────────┘
```

#### 4. **灵活支持不同页大小**

```
多级页表可以在同一进程中混合使用不同页大小：
• 代码段：4KB 页（精细控制）
• 数据库缓存：2MB 大页（减少 TLB 未命中）
• 大型数组：1GB 巨页（极大减少页表开销）

一级页表：只能用固定大小的页 ✗
```

### 📊 总结对比表

| 维度 | 一级页表 | 多级页表（4级） |
|------|---------|---------------|
| **内存开销** | ❌ 512 GB/进程 | ✅ 几十 KB - 几 MB/进程 |
| **速度（无 TLB）** | ✅ 100 ns | ❌ 400 ns |
| **速度（有 TLB，95% 命中）** | ✅ ~6 ns | ⚠️ ~21 ns |
| **稀疏地址空间支持** | ❌ 不支持 | ✅ 完美支持 |
| **动态按需分配** | ❌ 不支持 | ✅ 支持 |
| **混合页大小** | ❌ 困难 | ✅ 灵活支持 |
| **实际可行性** | ❌ 完全不可行 | ✅ 可行 |

### 💡 关键理解

1. **多级页表主要不是为了速度，而是为了节省内存**
   - 理论上比一级慢 4 倍
   - 实际上由于 TLB，性能差距很小（约 3.5 倍）
   - 但节省了 7,000,000 倍的内存！

2. **四级/五级页表 ≠ 大页拆分成小页**
   - 多级页表：查找结构（如何找到页表项）
   - 页大小：映射粒度（每次映射多大）
   - 两者独立，可任意组合

3. **Slab 确实是更细的颗粒度**
   - Buddy System：最小 4KB（物理页）
   - Slab：可小到几十字节（对象）
   - Slab 建立在 Buddy 之上

4. **为什么不用一级页表？**
   - 内存浪费极其严重（512 GB/进程）
   - 不支持稀疏地址空间
   - 不支持动态分配
   - 实际上完全不可行

---

## 四、书籍目录类比：直观理解多级页表

让我们用一个**具体的书籍例子**来说明多级页表如何工作，以及为什么节省空间。

### 📖 场景设定

假设有一本**百万页的百科全书**（类比 256TB 虚拟地址空间）：

```
书籍规格：
• 总页数：1,048,576 页（1024 × 1024 = 2^20 页）
• 每页大小：4KB
• 实际内容：只有 36 页有内容，其余都是空白页
  - 第 1-10 页：前言
  - 第 1000-1009 页：第一章
  - 第 50000-50009 页：第二章
  - 第 1000000-1000005 页：附录
```

这本书类比一个**进程的虚拟地址空间**：
- 总共可以访问 1,048,576 页（虚拟地址空间）
- 但实际只使用了 36 页（稀疏性）

### 🗂️ 方案 1：一级目录（线性目录）

**设计**：列出所有页的目录

```
┌──────────────────────────────────────────────────────┐
│ 《百万页百科全书》完整目录                            │
├──────────────────────────────────────────────────────┤
│ 第 1 页：前言第 1 页 ..................... 位置：书架 A-001 │
│ 第 2 页：前言第 2 页 ..................... 位置：书架 A-002 │
│ 第 3 页：前言第 3 页 ..................... 位置：书架 A-003 │
│ ...（中间 987 页空白，但目录仍要列出）                │
│ 第 991 页：（空白页）...................... 位置：无          │
│ 第 992 页：（空白页）...................... 位置：无          │
│ 第 993 页：（空白页）...................... 位置：无          │
│ ...                                                   │
│ 第 1000 页：第一章第 1 页 ................. 位置：书架 B-100 │
│ 第 1001 页：第一章第 2 页 ................. 位置：书架 B-101 │
│ ...（中间 48,990 页空白）                             │
│ 第 50000 页：第二章第 1 页 ................ 位置：书架 C-500 │
│ ...（中间 949,995 页空白）                            │
│ 第 1000000 页：附录第 1 页 ................ 位置：书架 Z-999 │
│ ...                                                   │
│ 第 1048576 页：（空白页）.................. 位置：无          │
└──────────────────────────────────────────────────────┘

目录条目总数：1,048,576 条
每条目录 8 字节（页号 4B + 位置指针 4B）
目录总大小：1,048,576 × 8 字节 = 8 MB

即使只有 36 页有内容，仍需 8 MB 目录！
```

**查找示例**：找第 50005 页

```
步骤：
1. 直接翻到目录第 50005 条
2. 读取：位置：书架 C-505
3. 去书架 C-505 取书

查找次数：1 次（直接索引）✓ 快
目录大小：8 MB（浪费）✗ 占空间
```

### 🗃️ 方案 2：四级目录（树状目录）

**设计**：部 → 卷 → 章 → 页

```
地址拆分（类比虚拟地址拆分）：
页号 = 50005（十进制）= 0x00C355（十六进制）
= 0000_0000_1100_0011_0101_0101（二进制，20位）

拆分成 4 级索引（每级 5 位，可索引 32 个条目）：
┌────────┬────────┬────────┬────────┐
│ [19:15]│ [14:10]│ [9:5]  │ [4:0]  │
│   0    │   6    │   6    │   21   │
│  部索引 │  卷索引 │ 章索引 │ 页索引 │
└────────┴────────┴────────┴────────┘

页号 50005 对应：
• 第 0 部（Part 0）
• 第 6 卷（Volume 6）
• 第 6 章（Chapter 6）
• 第 21 页（Page 21）
```

**目录结构**：

```
┌─────────────────────────────────────────────────────┐
│ 一级目录：部目录（总是存在，1 页 = 4KB）             │
├─────────────────────────────────────────────────────┤
│ 部 0 → 指向"卷目录 A"的位置（柜子 001）              │
│ 部 1 → NULL（这个部没内容，不建立卷目录）            │
│ 部 2 → NULL                                         │
│ ...                                                 │
│ 部 31 → 指向"卷目录 Z"的位置（柜子 100）             │
└─────────────────────────────────────────────────────┘
         ↓ 查询部 0
┌─────────────────────────────────────────────────────┐
│ 二级目录：卷目录 A（存储在柜子 001，1 页 = 4KB）     │
├─────────────────────────────────────────────────────┤
│ 卷 0 → 指向"章目录 A0"（柜子 002）                   │
│ 卷 1 → NULL                                         │
│ ...                                                 │
│ 卷 6 → 指向"章目录 A6"（柜子 008）                   │
│ ...                                                 │
│ 卷 31 → NULL                                        │
└─────────────────────────────────────────────────────┘
         ↓ 查询卷 6
┌─────────────────────────────────────────────────────┐
│ 三级目录：章目录 A6（存储在柜子 008，1 页 = 4KB）    │
├─────────────────────────────────────────────────────┤
│ 章 0 → NULL                                         │
│ ...                                                 │
│ 章 6 → 指向"页目录 A6-6"（柜子 009）                 │
│ ...                                                 │
│ 章 31 → NULL                                        │
└─────────────────────────────────────────────────────┘
         ↓ 查询章 6
┌─────────────────────────────────────────────────────┐
│ 四级目录：页目录 A6-6（存储在柜子 009，1 页 = 4KB）  │
├─────────────────────────────────────────────────────┤
│ 页 0 → NULL                                         │
│ ...                                                 │
│ 页 21 → 位置：书架 C-505                            │
│ ...                                                 │
│ 页 31 → NULL                                        │
└─────────────────────────────────────────────────────┘
```

**查找示例**：找第 50005 页

```
步骤：
1. 拆分页号：50005 → 部0 + 卷6 + 章6 + 页21
2. 查部目录：部0 → 柜子 001（卷目录 A）
3. 去柜子 001，查卷目录 A：卷6 → 柜子 008（章目录 A6）
4. 去柜子 008，查章目录 A6：章6 → 柜子 009（页目录 A6-6）
5. 去柜子 009，查页目录 A6-6：页21 → 书架 C-505
6. 去书架 C-505 取书

查找次数：4 次目录查询 + 1 次取书（总共 5 次）✗ 稍慢
目录大小：见下面计算 ✓ 节省空间
```

### 💾 目录空间对比

**一级目录**：

```
所有 1,048,576 页都要在目录中列出：
• 条目数：1,048,576 条
• 每条 8 字节
• 总大小：8,388,608 字节 = 8 MB

即使书只有 36 页内容，目录仍需 8 MB！
```

**四级目录**（只为有内容的部分建立目录）：

```
实际使用的页：
• 第 1-10 页：前言（部0, 卷0, 章0）
• 第 1000-1009 页：第一章（部0, 卷0, 章31）
• 第 50000-50009 页：第二章（部0, 卷6, 章6）
• 第 1000000-1000005 页：附录（部31, 卷6, 章6）

需要的目录：
┌─────────────────────────────────────────────────┐
│ 1 个部目录（总是需要）            = 1 页 = 4KB  │
├─────────────────────────────────────────────────┤
│ 2 个卷目录（部0 一个，部31 一个） = 2 页 = 8KB  │
├─────────────────────────────────────────────────┤
│ 4 个章目录：                                     │
│   • 部0-卷0：有章0 和章31                        │
│   • 部0-卷6：有章6                               │
│   • 部31-卷6：有章6                              │
│                                   = 4 页 = 16KB │
├─────────────────────────────────────────────────┤
│ 5 个页目录：                                     │
│   • 部0-卷0-章0（前言 10 页）                    │
│   • 部0-卷0-章31（第一章 10 页）                 │
│   • 部0-卷6-章6（第二章 10 页）                  │
│   • 部31-卷6-章6（附录 6 页）                    │
│   • （实际可能只需 4 个，简化计算用 5 个）        │
│                                   = 5 页 = 20KB │
└─────────────────────────────────────────────────┘

总计：12 页 = 48 KB

节省比例：8 MB / 48 KB = 170 倍！
```

### 🔍 为什么多级目录节省空间？

**关键**：**只为有内容的部分建立目录**

```
一级目录（强制列出所有页）：
┌──────────────────────────────────────────────┐
│ 第 1 页：有内容 ✓                             │
│ 第 2 页：有内容 ✓                             │
│ ...                                          │
│ 第 11 页：空白 ✗ ← 仍要在目录中占一条          │
│ 第 12 页：空白 ✗ ← 仍要在目录中占一条          │
│ ...（988 条空白页的目录条目）                 │
│ 第 1000 页：有内容 ✓                          │
│ ...                                          │
└──────────────────────────────────────────────┘

浪费：为 1,048,540 个空白页维护目录条目


多级目录（按需建立）：
┌──────────────────────────────────────────────┐
│ 部目录：只标记有内容的部（部0, 部31）          │
│   ↓                                          │
│ 卷目录：只为有内容的部建立                     │
│   • 部0 的卷目录：只标记卷0, 卷6               │
│   • 部31 的卷目录：只标记卷6                   │
│   ↓                                          │
│ 章目录：只为有内容的卷建立                     │
│   • 部0-卷0 的章目录：只标记章0, 章31          │
│   • 部0-卷6 的章目录：只标记章6                │
│   • 部31-卷6 的章目录：只标记章6               │
│   ↓                                          │
│ 页目录：只为有内容的章建立                     │
│   • 只建立 5 个页目录                          │
│   • 每个页目录只列出有内容的页                 │
└──────────────────────────────────────────────┘

节省：不为空白区域建立中间目录
```

### 📊 稀疏性的影响

**极端例子**：书只有 4 页有内容

```
页分布：
• 第 1 页（部0, 卷0, 章0, 页1）
• 第 10000 页（部0, 卷10, 章3, 页16）
• 第 500000 页（部15, 卷8, 章5, 页0）
• 第 1000000 页（部31, 卷6, 章6, 页0）

一级目录：
• 目录大小：8 MB（不变！）
• 即使只有 4 页，仍需完整目录

四级目录：
• 1 个部目录（4KB）
• 4 个卷目录（16KB）
  - 部0：标记卷0, 卷10
  - 部15：标记卷8
  - 部31：标记卷6
• 4 个章目录（16KB）
• 4 个页目录（16KB）
────────────────────
总计：52 KB

节省：8 MB / 52 KB = 157 倍

关键：目录大小与实际内容成正比，不是与总容量成正比！
```

### 🎯 多级目录如何定位到任意页？

**完整查找流程**：找第 50005 页

```
第 1 步：拆分页号
────────────────────────────────────────────
页号：50005 = 0x00C355
二进制：0000_0000_1100_0011_0101_0101
拆分：
  [19:15] = 00000 = 0   ← 部索引
  [14:10] = 00110 = 6   ← 卷索引
  [9:5]   = 00110 = 6   ← 章索引
  [4:0]   = 10101 = 21  ← 页索引


第 2 步：查部目录（总是在固定位置：主柜台）
────────────────────────────────────────────
去主柜台，翻开"部目录"：
  部 0 → 卷目录在"柜子 001"

读取：1 次


第 3 步：查卷目录（柜子 001）
────────────────────────────────────────────
去"柜子 001"，翻开"卷目录 A"：
  卷 6 → 章目录在"柜子 008"

读取：1 次


第 4 步：查章目录（柜子 008）
────────────────────────────────────────────
去"柜子 008"，翻开"章目录 A6"：
  章 6 → 页目录在"柜子 009"

读取：1 次


第 5 步：查页目录（柜子 009）
────────────────────────────────────────────
去"柜子 009"，翻开"页目录 A6-6"：
  页 21 → 书在"书架 C-505"

读取：1 次


第 6 步：取书
────────────────────────────────────────────
去"书架 C-505"，拿到第 50005 页

总查找次数：4 次目录查询 + 1 次取书 = 5 次
```

### ⚡ 速度 vs 空间的权衡

```
┌──────────────┬─────────────┬─────────────┐
│ 方案         │ 查找次数     │ 目录大小     │
├──────────────┼─────────────┼─────────────┤
│ 一级目录     │ 1 次 ✓ 快   │ 8 MB ✗ 大   │
│ 四级目录     │ 4 次 ✗ 慢   │ 48 KB ✓ 小  │
└──────────────┴─────────────┴─────────────┘

权衡：
• 牺牲一点速度（4 倍查找时间）
• 换取巨大空间节省（170 倍）
• 实际系统通过缓存（TLB）弥补速度差距
```

### 💡 对应到页表的映射

| 书籍概念 | 页表概念 | 说明 |
|---------|---------|------|
| **部目录** | PML4 表 | 顶级索引，总是存在 |
| **卷目录** | PDPT 表 | 第二级索引 |
| **章目录** | PD 表 | 第三级索引 |
| **页目录** | PT 表 | 第四级索引 |
| **书的位置** | 物理页框号 | 最终的物理地址 |
| **页号** | 虚拟地址 | 要查找的地址 |
| **柜子位置** | 页表的物理地址 | 存储位置 |
| **主柜台** | CR3 寄存器 | 顶级目录的位置 |

**完整类比**：

```
书籍查找：
页号 → 拆分 → 部目录 → 卷目录 → 章目录 → 页目录 → 书架位置
       索引     ↑ 主柜台（总是知道这个位置）

页表查找：
虚拟地址 → 拆分 → PML4 → PDPT → PD → PT → 物理页框
          索引     ↑ CR3（总是知道这个地址）
```

### 📝 总结

**如何定位到任意页？**
1. 将页号拆分成多级索引（部/卷/章/页）
2. 从顶级目录（主柜台）开始，逐级查找
3. 每一级指向下一级目录的存储位置（柜子号）
4. 最后一级指向实际内容的位置（书架号）

**如何节省目录空间？**
1. **按需建立目录**：只为有内容的部分建立中间目录
2. **稀疏性利用**：空白区域不建立目录，用 NULL 标记
3. **目录大小与内容成正比**：而不是与总容量成正比
4. **动态增长**：需要时才分配新的目录页

**关键洞察**：
- 一级目录：8 MB（无论实际内容多少）
- 四级目录：48 KB（36 页内容）→ 52 KB（4 页内容）
- 节省比例：170 倍（内容多）→ 157 倍（内容少）
- 多级目录的开销随实际使用量线性增长

这就是为什么现代操作系统都使用多级页表，而不是一级页表！

---

## 五、页表的动态管理：内核如何读写目录

### ❓ 核心问题

用户问："所以 kernel 还要不停读取和写入这些页信息，对吧？因为书籍的内容（虚拟地址的内容和 process 的变化）会不断变化？"

**答案**：是的！但要区分**两种不同的操作**。

### 📖 操作1：读取页表（CPU 硬件自动完成）

**谁在读？** CPU 的 MMU（内存管理单元），**不是内核代码**

**什么时候读？** **每次内存访问**（读数据、写数据、取指令）

```
程序执行：mov %rax, (%rbx)   # 读取 %rbx 指向的内存

CPU 硬件自动执行（对程序员和内核都透明）：
┌─────────────────────────────────────────────────┐
│ 1. 读取 %rbx = 虚拟地址 0x00400000             │
│                                                 │
│ 2. MMU 自动查询 TLB：                           │
│    TLB[0x00400000] 命中？                       │
│    ├─ 是：直接返回物理地址（<1ns）✓             │
│    └─ 否：查询多级页表 ↓                        │
│                                                 │
│ 3. MMU 查询多级页表（硬件自动）：               │
│    CR3 → PML4[1] → PDPT[0] → PD[0] → PT[0]    │
│    → 物理页框 0x12345000                        │
│                                                 │
│ 4. 更新 TLB：                                   │
│    TLB[0x00400000] = 0x12345000                │
│                                                 │
│ 5. 访问物理地址：                               │
│    物理地址 = 0x12345000 + 0（页内偏移）        │
│             = 0x12345000                        │
│    读取数据到 %rax                              │
└─────────────────────────────────────────────────┘

关键：这个过程完全自动，内核代码不参与！
```

**书籍类比**：

```
读者（程序）说："我要看第 50005 页"

图书管理员（MMU）自动工作：
1. 查记忆（TLB）："第 50005 页在书架 C-505"
   ├─ 记得：直接去书架 C-505 取书 ✓
   └─ 不记得：查目录 ↓

2. 查多级目录（自动）：
   部目录 → 卷目录 → 章目录 → 页目录 → 书架 C-505

3. 记住这次查询结果（更新 TLB）

4. 去书架 C-505 取书给读者

读者不需要自己查目录！管理员自动完成所有工作！
```

**频率**：

```
典型程序：
• 每秒执行：10 亿条指令（1 GHz CPU）
• 每条指令平均：1-2 次内存访问
• 每秒内存访问：10-20 亿次

如果没有 TLB：
• 每次访问查 4 级页表 = 40-80 亿次页表查询/秒
• CPU 会被页表查询拖垮！

有了 TLB（90-99% 命中率）：
• 实际页表查询：1-10 亿次/秒（未命中的 1-10%）
• 大部分时间零开销（直接从 TLB 读取）
• 性能可接受 ✓
```

### ✏️ 操作2：写入/修改页表（内核主动管理）

**谁在写？** 内核代码（`arch/x86/mm/` 和 `mm/` 子系统）

**什么时候写？** 进程生命周期的关键事件

**场景1：进程创建（fork）**

```
父进程调用 fork()：
┌──────────────────────────────────────────────┐
│ 父进程的页表：                                │
│ • 代码段：第 1-100 页 → 书架 A 区             │
│ • 数据段：第 200-300 页 → 书架 B 区           │
│ • 堆：第 500-600 页 → 书架 C 区               │
│ • 栈：第 1000-1010 页 → 书架 D 区             │
└──────────────────────────────────────────────┘
                ↓ fork()
┌──────────────────────────────────────────────┐
│ 内核操作（写入页表）：                        │
│ 1. 为子进程分配新的部目录（PML4）             │
│ 2. 复制父进程的所有目录（PML4/PDPT/PD/PT）   │
│ 3. 设置写时复制（COW）：                      │
│    • 将所有页表项标记为只读（R/W=0）          │
│    • 父子进程共享物理页（暂时）               │
│ 4. 子进程的 CR3 指向新的 PML4                 │
└──────────────────────────────────────────────┘
```

**书籍类比**：图书馆来了新读者，复制一份目录

```
父进程 fork()：

管理员（内核）：
1. 给儿子复制一份完整的目录（页表）
2. 但书架上的书暂时共享（物理页共享）
3. 在两份目录上都标记"只读"（R/W=0）
4. 将来谁要修改，谁就复制一本新书（写时复制）
```

**场景2：内存分配（malloc/mmap）**

```
程序调用：ptr = malloc(8192);  // 分配 2 个 4KB 页

┌──────────────────────────────────────────────┐
│ 内核操作（懒分配）：                          │
│                                               │
│ 1. 查找未使用的虚拟地址范围                    │
│    假设找到：0x00600000 - 0x00601FFF          │
│                                               │
│ 2. 更新进程的 VMA（虚拟内存区域）列表          │
│    记录：0x00600000-0x00601FFF 已分配         │
│                                               │
│ 3. 暂时不建立页表映射！                        │
│    此时访问 0x00600000 会触发缺页异常          │
└──────────────────────────────────────────────┘
                ↓ 程序首次访问 ptr
┌──────────────────────────────────────────────┐
│ 缺页异常处理（Page Fault Handler）：         │
│                                               │
│ 1. 检查 VMA：0x00600000 是合法地址吗？        │
│    是 → 继续                                  │
│    否 → Segmentation Fault                    │
│                                               │
│ 2. 从 Buddy System 分配物理页：               │
│    分配：物理页框 0x05000000                  │
│                                               │
│ 3. 建立页表映射（写入页表）：                  │
│    • 分配 PT 表（如果不存在）                 │
│    • 更新 PT 表项：                           │
│      PT[0] = 0x05000007 (P=1,RW=1,US=1)      │
│    • 刷新 TLB：invlpg 0x00600000              │
│                                               │
│ 4. 返回用户态，重新执行触发异常的指令          │
└──────────────────────────────────────────────┘
```

**书籍类比**：读者要新书，管理员更新目录

```
读者："我要预订 2 本新书，地址是 600-601"

管理员（malloc，懒分配）：
• 在预订本上记录："600-601 已预订"
• 但不立即准备书

读者："我要看第 600 页"

管理员（缺页异常）：
• 从仓库拿一本新书（分配物理页）
• 放在书架 E-001
• 更新目录："第 600 页 → 书架 E-001"
• 刷新记忆（TLB）
• 告诉读者："好了，去书架 E-001 拿书"
```

**场景3：内存释放（munmap/free）**

```
程序调用：munmap(ptr, 8192);

┌──────────────────────────────────────────────┐
│ 内核操作（写入页表）：                        │
│                                               │
│ 1. 查找对应的页表项                           │
│    虚拟地址 0x00600000-0x00601FFF             │
│    → PT[0] 和 PT[1]                           │
│                                               │
│ 2. 读取页表项，获取物理页框号：                │
│    PT[0] = 0x05000007 → 物理页框 0x05000000   │
│    PT[1] = 0x05001007 → 物理页框 0x05001000   │
│                                               │
│ 3. 删除页表项（写入页表）：                    │
│    PT[0] = 0 (P=0, 无效)                      │
│    PT[1] = 0 (P=0, 无效)                      │
│                                               │
│ 4. 归还物理页给 Buddy System：                │
│    free_pages(0x05000000, 2);                 │
│                                               │
│ 5. 刷新 TLB：                                 │
│    invlpg 0x00600000                          │
│    invlpg 0x00601000                          │
│                                               │
│ 6. 如果 PT 表变空，可能释放 PT 表本身          │
└──────────────────────────────────────────────┘
```

**书籍类比**：读者归还书，管理员删除目录条目

```
读者："我要归还第 600-601 页"

管理员（munmap）：
1. 查目录："第 600 页在书架 E-001"
2. 去书架 E-001 收回书
3. 删除目录条目："第 600 页：删除"
4. 清理记忆（刷新 TLB）
5. 把书放回仓库（归还 Buddy System）
6. 如果整个"页目录"都空了，扔掉这页目录
```

**场景4：写时复制（Copy-On-Write, COW）**

```
fork() 后，父子进程共享物理页（只读）

子进程写入共享页：
┌──────────────────────────────────────────────┐
│ 子进程执行：*ptr = 123;  // ptr 指向共享页    │
│                                               │
│ CPU 检测到写保护：                             │
│ • 页表项：R/W=0 (只读)                         │
│ • 触发 Page Fault (#PF)                       │
└──────────────────────────────────────────────┘
                ↓
┌──────────────────────────────────────────────┐
│ 内核 COW 处理（do_wp_page）：                 │
│                                               │
│ 1. 检查：这是 COW 页吗？                      │
│    是 → 继续                                  │
│    否 → Segmentation Fault                    │
│                                               │
│ 2. 分配新的物理页：                           │
│    新页框：0x06000000                         │
│                                               │
│ 3. 复制旧页内容到新页：                        │
│    memcpy(0x06000000, 0x05000000, 4096);     │
│                                               │
│ 4. 更新子进程的页表项（写入页表）：            │
│    PT[0] = 0x06000007 (P=1, RW=1, US=1)      │
│    现在指向新页，可写                          │
│                                               │
│ 5. 减少旧页的引用计数：                        │
│    如果父进程是唯一引用者，父进程页表也改为可写 │
│                                               │
│ 6. 刷新 TLB                                   │
└──────────────────────────────────────────────┘
```

**书籍类比**：父子共享书，儿子要修改时才复制

```
fork() 后：
• 父子都拿着"第 500 页"的借书证
• 但都只能看，不能写

子进程："我要在第 500 页上写字"

管理员（发现写保护）：
1. "这是共享的书，不能直接写！"
2. 复印一本新书（分配新物理页）
3. 把新书给儿子："这本是你的，随便写"
4. 更新儿子的目录："第 500 页 → 新书架 F-001（可写）"
5. 父亲的目录改为可写（因为现在只有父亲用旧书）
```

**场景5：页交换（Swap）**

```
内存紧张，内核决定交换出一些页：
┌──────────────────────────────────────────────┐
│ 内核操作（kswapd 守护进程）：                  │
│                                               │
│ 1. 选择牺牲页（LRU 算法）：                    │
│    选中：虚拟地址 0x00800000（物理 0x07000000）│
│                                               │
│ 2. 将页内容写入磁盘交换区：                    │
│    swap_out(0x07000000) → 交换槽 #12345       │
│                                               │
│ 3. 修改页表项（写入页表）：                    │
│    旧：PT[0] = 0x07000007 (P=1, 在内存)       │
│    新：PT[0] = 0x00012345 (P=0, 在磁盘)       │
│         └─ 高位存储交换槽号                   │
│                                               │
│ 4. 归还物理页给 Buddy System：                │
│    free_page(0x07000000);                     │
│                                               │
│ 5. 刷新 TLB                                   │
└──────────────────────────────────────────────┘
                ↓ 程序访问被交换出的页
┌──────────────────────────────────────────────┐
│ 缺页异常（换入）：                             │
│                                               │
│ 1. 检查 PT[0] = 0x00012345，P=0              │
│    但高位非零 → 在交换区                       │
│                                               │
│ 2. 分配新的物理页：0x08000000                │
│                                               │
│ 3. 从磁盘读取：                               │
│    swap_in(#12345) → 0x08000000              │
│                                               │
│ 4. 修改页表项（写入页表）：                    │
│    PT[0] = 0x08000007 (P=1, 在内存)          │
│                                               │
│ 5. 释放交换槽 #12345                          │
│                                               │
│ 6. 刷新 TLB，返回用户态                       │
└──────────────────────────────────────────────┘
```

**书籍类比**：书架满了，把不常用的书放到地下仓库

```
书架满了：
1. 管理员把不常用的"第 800 页"放到地下仓库
2. 更新目录："第 800 页 → 地下仓库 #12345 格"
3. 书架位置空出来

读者："我要看第 800 页"

管理员：
1. 查目录："第 800 页在地下仓库 #12345"
2. 去地下仓库拿书（慢！可能几毫秒）
3. 放到书架 E-010
4. 更新目录："第 800 页 → 书架 E-010"
5. "好了，去 E-010 拿书"
```

### 📊 读写频率对比

```
┌──────────────────┬────────────┬──────────────┐
│ 操作类型         │ 执行者      │ 频率          │
├──────────────────┼────────────┼──────────────┤
│ 读取页表         │ CPU MMU    │ 极高          │
│ （硬件自动）     │ （硬件）   │ 10-20 亿次/秒 │
│                  │            │ （有 TLB 优化）│
├──────────────────┼────────────┼──────────────┤
│ 修改页表         │ 内核代码   │ 相对较低      │
│ （软件主动）     │ （mm 子系统）│ 按需触发     │
└──────────────────┴────────────┴──────────────┘
```

**修改页表的触发事件**：

| 事件 | 频率 | 页表操作 |
|------|------|---------|
| fork() | 每秒几次 | 复制整个页表 |
| malloc() | 每秒几百次 | 懒分配（首次访问才修改）|
| 缺页异常 | 每秒几千次 | 建立映射 |
| munmap() | 每秒几十次 | 删除映射 |
| COW | 每秒几百次 | 修改权限、复制页 |
| Swap out/in | 内存紧张时 | 修改 P 位 |
| exit() | 每秒几次 | 删除整个页表 |

### 💡 关键理解

1. **读取页表 vs 修改页表**：
   - 读取：CPU 硬件自动（MMU），频率极高（亿次/秒）
   - 修改：内核代码主动（mm 子系统），频率较低（按需触发）

2. **TLB 的重要性**：
   - 没有 TLB：每次内存访问查 4 次页表 = 不可接受
   - 有 TLB：90-99% 命中 = 大部分时间零开销

3. **懒分配（Lazy Allocation）**：
   - malloc() 不立即建立映射
   - 首次访问时才缺页，才修改页表
   - 节省内存和时间

4. **书籍类比的准确性**：
   - CPU = 读者（自己查目录取书，管理员自动帮忙）
   - 内核 = 管理员（管理目录、分配书架）
   - TLB = 读者的记忆（记住常看的书在哪）
   - 页表 = 图书目录（管理员不断更新）

5. **为什么要动态管理？**：
   - 进程创建/销毁：目录要增减
   - 内存分配/释放：映射要变化
   - 内存共享（fork）：多个进程共享物理页
   - 内存交换（swap）：物理内存有限，需要磁盘辅助
   - 写时复制（COW）：优化 fork 性能

这就是为什么页表是一个**动态的、不断变化的**数据结构！

### 📚 深入阅读

- 缺页异常处理：LINUX_MEMORY_MANAGEMENT_EVOLUTION.md 第三部分
- 写时复制机制：LINUX_MEMORY_MANAGEMENT_EVOLUTION.md 第三部分
- Buddy System 和 Slab 详解：[伙伴系统与 Slab 分配器详解](BUDDY_ALLOCATOR_GUIDE.md)
- 多级页表实现细节：[Linux 内核分页机制完整指南](LINUX_MEMORY_MANAGEMENT_EVOLUTION.md)
- TLB 优化和性能分析：LINUX_MEMORY_MANAGEMENT_EVOLUTION.md 第一部分
